openapi: 3.0.0
info:
  title: Kawan Nusa API
  description: Official API Documentation for Kawan Nusa Backend Service. Includes all endpoints for Authentication, Profile, Points, Customers, Rewards, Redemptions, Statistics, Education, Catalog, and more.
  version: 1.0.0
servers:
  - url: http://localhost:8000/api
    description: Local development server
  - url: /api
    description: Relative API server

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: "JWT access token. Expires in 15 minutes."
    apiKeyAuth:
      type: apiKey
      in: header
      name: X-API-KEY
      description: "Internal API key for server-to-server communication."

  schemas:
    # --- Base Wrapper Schemas ---
    BaseResponse:
      type: object
      properties:
        success: { type: boolean, example: true }
        statusCode: { type: integer, example: 200 }
        message: { type: string, example: "Operation successful" }

    PaginationMeta:
      type: object
      properties:
        total: { type: integer, example: 100 }
        perPage: { type: integer, example: 10 }
        currentPage: { type: integer, example: 1 }
        lastPage: { type: integer, example: 10 }
        from: { type: integer, example: 1 }
        to: { type: integer, example: 10 }

    ValidationError:
      type: object
      properties:
        success: { type: boolean, example: false }
        statusCode: { type: integer, example: 422 }
        message: { type: string, example: "Validation failed" }
        errors:
          type: object
          example:
            email: "Invalid email format"
            password: "Password must be at least 6 characters"

    # --- Auth Schemas ---
    RegisterRequest:
      type: object
      required: [firstName, lastName, birthDate, birthPlace, identityNumber, taxNumber, email, phone, address, bankName, accountNumber, accountHolderName, identity, account]
      properties:
        firstName: { type: string, example: "Budi" }
        lastName: { type: string, example: "Santoso" }
        birthDate: { type: string, format: date, example: "1990-05-15" }
        birthPlace: { type: string, example: "Surabaya" }
        identityNumber: { type: string, example: "3201234567890001" }
        taxNumber: { type: string, example: "01.234.567.8-901.000" }
        email: { type: string, format: email, example: "budi.santoso@nusanet.id" }
        phone: { type: string, example: "081234567890" }
        address: { type: string, example: "Jl. Raya Darmo No. 10, Surabaya" }
        bankName: { type: string, example: "BCA" }
        accountNumber: { type: string, example: "1234567890" }
        accountHolderName: { type: string, example: "Budi Santoso" }
        company: { type: string, nullable: true, example: "PT Nusanet" }
        jobPosition: { type: string, nullable: true, example: "Sales Manager" }
        companyAddress: { type: string, nullable: true, example: "Jl. Ngagel Jaya No. 88, Surabaya" }
        isWhatsapp: { type: boolean, example: true, description: "Whether the phone number is connected to WhatsApp" }
        identity:
          type: string
          format: binary
          description: "Identity document image (KTP). JPG/PNG/GIF. Max 5MB."
        account:
          type: string
          format: binary
          description: "Account book image (Buku Rekening). JPG/PNG/GIF. Max 5MB."

    LoginRequest:
      type: object
      required: [identifier, password]
      properties:
        identifier:
          type: string
          description: "Email address or phone number"
          example: "budi.santoso@nusanet.id"
        password: { type: string, example: "secret123" }

    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email:
          { type: string, format: email, example: "budi.santoso@nusanet.id" }

    ResetPasswordRequest:
      type: object
      required: [token, newPassword]
      properties:
        token: { type: string, example: "a3f8c2d1e9b4..." }
        newPassword: { type: string, minLength: 6, example: "newSecret123" }

    RefreshTokenRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken:
          { type: string, example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." }

    # --- Auth User Schema ---
    AuthUser:
      type: object
      properties:
        id: { type: integer, example: 1 }
        name: { type: string, example: "Budi Santoso" }
        photo:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/photo.jpg",
          }
        email:
          { type: string, format: email, nullable: true, example: "budi.santoso@nusanet.id" }
        phone: { type: string, nullable: true, example: "081234567890" }
        status: { type: string, enum: [pending, active, inactive, reject, revision], nullable: true, example: active }
        statusNote: { type: string, nullable: true, example: "Foto KTP kurang jelas", description: "Catatan dari admin saat approve/revisi/tolak" }
        isVerified: { type: boolean, example: true, description: "Apakah email sudah diverifikasi" }
        isBoarding: { type: boolean, example: true, description: "Apakah sudah menyelesaikan onboarding" }
        role:
          type: string
          enum: ["user", "admin"]
          example: "user"

    UserProfile:
      type: object
      properties:
        id: { type: integer, example: 1 }
        firstName: { type: string, example: "Budi" }
        lastName: { type: string, example: "Santoso" }
        photo:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/photo.jpg",
          }
        email:
          { type: string, format: email, nullable: true, example: "budi.santoso@nusanet.id" }
        phone: { type: string, nullable: true, example: "081234567890" }
        hasWhatsapp: { type: boolean, example: true }
        identityNumber: { type: integer, nullable: true, example: 3201234567890001 }
        taxNumber: { type: string, nullable: true, example: "01.234.567.8-901.000" }
        company: { type: string, example: "PT Nusanet" }
        jobPosition: { type: string, example: "Sales Manager" }
        birthDate: { type: string, format: date, nullable: true, example: "1990-05-15" }
        birthPlace: { type: string, nullable: true, example: "Surabaya" }
        address: { type: string, nullable: true, example: "Jl. Raya Darmo No. 10, Surabaya" }
        companyAddress: { type: string, nullable: true, example: "Jl. Ngagel Jaya No. 88, Surabaya" }
        identityPath:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/identity/1_1234567890.jpg",
          }
        bankDetails:
          type: object
          properties:
            holderName:
              { type: string, nullable: true, example: "Budi Santoso" }
            name: { type: string, nullable: true, example: "BCA" }
            number: { type: string, nullable: true, example: "1234567890" }
            accountPath:
              {
                type: string,
                nullable: true,
                example: "https://cdn.example.com/account/1_1234567890.jpg",
              }
        settings:
          type: object
          properties:
            isSubscribe: { type: boolean, example: true }
            isAutoWithdraw: { type: boolean, example: false }
        isVerified: { type: boolean, example: true }
        isBoarding: { type: boolean, example: false }
        status: { type: string, enum: [pending, active, inactive, reject, revision], nullable: true, example: active }
        statusNote: { type: string, nullable: true, example: "Data lengkap dan valid." }
        statusUpdatedAt: { type: string, format: date-time, nullable: true, example: "2025-01-15T10:30:00.000Z" }
        lastLoginAt:
          {
            type: string,
            format: date-time,
            nullable: true,
            example: "2025-06-24T10:30:00.000Z",
          }
        passwordUpdatedAt:
          {
            type: string,
            format: date-time,
            nullable: true,
            example: "2025-01-15T10:30:00.000Z",
          }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-06-01T08:00:00.000Z",
          }

    AuthResponse:
      type: object
      properties:
        user: { $ref: "#/components/schemas/AuthUser" }
        accessToken:
          type: string
          description: "JWT access token, expires in 15 minutes"
          example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsImVtYWlsIjoiYnVkaUBleGFtcGxlLmNvbSJ9..."
        refreshToken:
          type: string
          description: "JWT refresh token, expires in 7 days"
          example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjF9..."

    UpdateAccountRequest:
      type: object
      required: [firstName, lastName]
      properties:
        firstName: { type: string, example: "Budi" }
        lastName: { type: string, example: "Santoso" }
        email:
          { type: string, format: email, nullable: true, example: "budi.santoso@nusanet.id" }
        phone: { type: string, nullable: true, example: "081234567890" }
        hasWhatsapp: { type: boolean, example: true }
        identityNumber: { type: integer, nullable: true, example: 3201234567890001 }
        taxNumber: { type: string, nullable: true, example: "01.234.567.8-901.000" }
        company: { type: string, example: "PT Nusanet" }
        jobPosition: { type: string, example: "Sales Manager" }
        birthDate: { type: string, format: date, nullable: true, example: "1990-05-15" }
        birthPlace: { type: string, nullable: true, example: "Surabaya" }
        address: { type: string, nullable: true, example: "Jl. Raya Darmo No. 10, Surabaya" }
        companyAddress: { type: string, nullable: true, example: "Jl. Ngagel Jaya No. 88, Surabaya" }

    UpdateBankRequest:
      type: object
      required: [accountHolderName, bankName, accountNumber]
      properties:
        accountHolderName: { type: string, example: "Budi Santoso" }
        bankName: { type: string, example: "BCA" }
        accountNumber: { type: string, example: "1234567890" }

    UpdatePasswordRequest:
      type: object
      required: [oldPassword, newPassword]
      properties:
        oldPassword: { type: string, example: "oldSecret123" }
        newPassword: { type: string, minLength: 6, example: "newSecret456" }

    UpdatePreferenceRequest:
      type: object
      properties:
        isSubscribe: { type: boolean, example: true }
        isAutoWithdraw: { type: boolean, example: false }

    # --- Customer Schemas ---
    Customer:
      type: object
      properties:
        id: { type: string, example: "CUST-0001" }
        name: { type: string, example: "Warnet Jaya" }
        company: { type: string, example: "CV Jaya Abadi" }
        type: { type: string, example: "Warnet" }
        registrationDate: { type: string, format: date, example: "2024-01-15" }
        isActive: { type: boolean, example: true }
        phones:
          type: array
          items:
            type: object
            properties:
              id: { type: integer, example: 1 }
              phone: { type: string, example: "081234567890" }
              label: { type: string, example: "Primary" }
        emails:
          type: array
          items:
            type: object
            properties:
              id: { type: integer, example: 1 }
              email: { type: string, example: "warnet.jaya@example.com" }
              label: { type: string, example: "Primary" }

    CustomerDetail:
      allOf:
        - $ref: "#/components/schemas/Customer"
        - type: object
          properties:
            totalCustomerServices: { type: integer, example: 3 }
            latestCustomerService:
              nullable: true
              allOf:
                - $ref: "#/components/schemas/CustomerServiceItem"

    CustomerServiceItem:
      type: object
      properties:
        id: { type: integer, example: 42 }
        registrationDate:
          { type: string, format: date, nullable: true, example: "2024-01-10" }
        activationDate:
          { type: string, format: date, nullable: true, example: "2024-01-15" }
        address:
          { type: string, nullable: true, example: "Jl. Raya No.10, Surabaya" }
        startDate:
          { type: string, format: date, nullable: true, example: "2024-01-15" }
        endDate:
          { type: string, format: date, nullable: true, example: "2025-01-15" }
        status: { type: string, example: "AC" }
        referenceDate:
          { type: string, format: date, nullable: true, example: "2024-03-01" }
        sales:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 10 }
            name: { type: string, example: "Andi" }
            employeeId: { type: string, example: "EMP-001" }
        service:
          nullable: true
          type: object
          properties:
            code: { type: string, example: "SVC-FIBER-001" }
            name: { type: string, example: "Fiber Home 20 Mbps" }
            type: { type: string, example: "Internet" }
        customer:
          nullable: true
          type: object
          properties:
            id: { type: string, example: "CUST-0001" }
            name: { type: string, example: "Warnet Jaya" }
            company: { type: string, example: "CV Jaya Abadi" }
        totalPoint: { type: number, example: 1500 }
        latestPoint:
          nullable: true
          type: object
          properties:
            point: { type: number, example: 500 }
            type: { type: string, example: "OTP" }
            createdAt:
              {
                type: string,
                format: date-time,
                example: "2024-03-01T09:00:00.000Z",
              }

    # --- Service Schemas ---
    Service:
      type: object
      properties:
        id: { type: integer, example: 1 }
        code: { type: string, example: "SVC-FIBER-001" }
        name: { type: string, example: "Fiber Home 20 Mbps" }
        description:
          {
            type: string,
            example: "Internet fiber optic untuk rumahan, kecepatan 20 Mbps",
          }
        type: { type: string, example: "Internet" }
        isActive: { type: boolean, example: true }
        price: { type: number, example: 250000 }
        unit: { type: string, example: "Monthly" }
        category: { type: string, example: "Access Home" }
        features:
          type: array
          items: { type: string }
          example: ["Speed 20 Mbps", "Unlimited Quota", "24/7 Support"]
        url:
          {
            type: string,
            nullable: true,
            example: "https://nusanet.id/products/fiber-home-20",
          }
        lastReferanceDate:
          { type: string, format: date, nullable: true, example: "2024-03-15" }
        totalCustomerServices: { type: integer, example: 25 }
        totalPoint: { type: number, example: 12500 }

    # --- Point (Reward) Schemas ---
    Point:
      type: object
      properties:
        id: { type: integer, example: 101 }
        price: { type: number, example: 250000 }
        expiredDate: { type: string, format: date, example: "2025-03-01" }
        point: { type: number, example: 500 }
        remainingPoint: { type: number, example: 500 }
        isExpired: { type: boolean, example: false }
        type:
          { type: string, enum: ["OTP", "Bulanan", "Cashback"], example: "OTP" }
        customerService:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 42 }
            serviceCode: { type: string, example: "SVC-FIBER-001" }
            activationDate:
              {
                type: string,
                format: date,
                nullable: true,
                example: "2024-01-15",
              }
            startDate:
              {
                type: string,
                format: date,
                nullable: true,
                example: "2024-01-15",
              }
            endDate:
              {
                type: string,
                format: date,
                nullable: true,
                example: "2025-01-15",
              }
        service:
          nullable: true
          type: object
          properties:
            code: { type: string, example: "SVC-FIBER-001" }
            name: { type: string, example: "Fiber Home 20 Mbps" }
        customer:
          nullable: true
          type: object
          properties:
            id: { type: string, example: "CUST-0001" }
            name: { type: string, example: "Warnet Jaya" }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-03-01T09:00:00.000Z",
          }

    CreatePointRequest:
      type: object
      required: [customerServiceId, userId, price, point, type]
      properties:
        customerServiceId: { type: integer, minimum: 1, example: 42 }
        userId: { type: integer, minimum: 1, example: 5, description: "The referral user ID" }
        price: { type: number, minimum: 0, example: 250000 }
        point: { type: number, minimum: 0, example: 500 }
        type:
          type: string
          enum: ["OTP", "Bulanan", "Cashback"]
          example: "OTP"

    # --- Redemption Schemas ---
    Redemption:
      type: object
      properties:
        id: { type: integer, example: 1 }
        redempNo: { type: string, example: "RED-20240301-0001" }
        pointsUsed: { type: number, example: 1000 }
        type:
          {
            type: string,
            enum: ["cash", "voucher", "product"],
            example: "cash",
          }
        status:
          {
            type: string,
            enum: ["pending", "processing", "completed", "cancelled"],
            example: "pending",
          }
        notes:
          { type: string, nullable: true, example: "Mohon segera diproses" }
        withdrawDetails:
          nullable: true
          type: object
          properties:
            bankName: { type: string, example: "BCA" }
            accountNumber: { type: string, example: "1234567890" }
            accountHolderName: { type: string, example: "Budi Santoso" }
            payout: { type: number, example: 95000 }
            tax: { type: number, example: 5000 }
        voucherDetails:
          nullable: true
          type: object
          properties:
            catalog:
              nullable: true
              type: object
              properties:
                id: { type: integer, example: 5 }
                name: { type: string, example: "Voucher Tokopedia 50K" }
                image:
                  {
                    type: string,
                    example: "https://cdn.example.com/voucher.jpg",
                  }
                category:
                  nullable: true
                  type: object
                  properties:
                    id: { type: integer, example: 2 }
                    name: { type: string, example: "E-Commerce" }
            name: { type: string, example: "Budi Santoso" }
            email: { type: string, example: "budi.santoso@nusanet.id" }
            detail:
              nullable: true
              type: object
              properties:
                id: { type: integer, example: 10 }
                code: { type: string, example: "VCHRTOKO50K-ABCDE" }
                expiredDate:
                  { type: string, format: date, example: "2024-12-31" }
        productDetails:
          nullable: true
          type: object
          properties:
            catalog:
              nullable: true
              type: object
              properties:
                id: { type: integer, example: 7 }
                name: { type: string, example: "Mug Kawan Nusa" }
                image:
                  { type: string, example: "https://cdn.example.com/mug.jpg" }
                category:
                  nullable: true
                  type: object
                  properties:
                    id: { type: integer, example: 3 }
                    name: { type: string, example: "Merchandise" }
            name: { type: string, example: "Budi Santoso" }
            email: { type: string, example: "budi.santoso@nusanet.id" }
            phone: { type: string, example: "081234567890" }
            address: { type: string, example: "Jl. Raya No.10, Surabaya 60111" }
            shipping:
              nullable: true
              type: object
              properties:
                id: { type: integer, example: 3 }
                shipper: { type: string, example: "jne" }
                trackingNumber: { type: string, example: "JNE001234567890" }
                shippedAt:
                  {
                    type: string,
                    format: date-time,
                    example: "2024-03-05T14:00:00.000Z",
                  }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-03-01T09:00:00.000Z",
          }

    CreateCashRedemptionRequest:
      type: object
      required: [pointsUsed]
      properties:
        pointsUsed: { type: number, minimum: 1, example: 1000 }
        notes: { type: string, example: "Mohon segera diproses" }

    CreateRedemptionVoucherRequest:
      type: object
      required: [catalogId]
      properties:
        catalogId: { type: integer, example: 5 }
        notes: { type: string, example: "Voucher untuk hadiah" }

    CreateRedemptionProductRequest:
      type: object
      required: [catalogId, address]
      properties:
        catalogId: { type: integer, example: 7 }
        address:
          {
            type: string,
            example: "Jl. Raya No.10 RT 01/02, Kel. Tegalsari, Kec. Tegalsari, Surabaya 60111",
          }
        notes: { type: string, example: "Tolong dikirim pagi hari" }

    # --- Catalog Schemas ---
    Catalog:
      type: object
      properties:
        id: { type: integer, example: 5 }
        name: { type: string, example: "Voucher Tokopedia 50K" }
        type: { type: string, enum: ["product", "voucher"], example: "voucher" }
        description:
          {
            type: string,
            example: "Voucher belanja Tokopedia senilai Rp 50.000",
          }
        point: { type: number, example: 500 }
        image:
          {
            type: string,
            example: "https://cdn.example.com/voucher-tokopedia.jpg",
          }
        stock: { type: integer, example: 47, description: "Sisa stok (remaining)" }
        stockUsed: { type: integer, example: 3, description: "Jumlah yang sudah ditukar" }
        expiredDate: { type: string, format: date, nullable: true, example: "2025-12-31" }
        category:
          type: object
          properties:
            id: { type: integer, example: 2 }
            name: { type: string, example: "E-Commerce" }
        createdBy:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 1 }
            name: { type: string, example: "Admin Nusanet" }
            employeeId: { type: string, example: "EMP-001" }

    CatalogStockHistory:
      type: object
      properties:
        id: { type: integer, example: 1 }
        catalogId: { type: integer, example: 5 }
        type: { type: string, enum: ["initial", "addition", "reduction", "opname", "redemption"], example: "redemption" }
        quantity: { type: integer, example: -1, description: "Delta (positive = add, negative = subtract)" }
        stockBefore: { type: integer, example: 48 }
        stockAfter: { type: integer, example: 47 }
        notes: { type: string, nullable: true, example: "Penukaran voucher oleh user #1" }
        createdBy:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 1 }
            name: { type: string, example: "Admin Nusanet" }
        createdAt: { type: string, format: date-time, example: "2024-06-01T08:00:00.000Z" }

    # --- Education Schemas ---
    EducationArticle:
      type: object
      properties:
        id: { type: integer, example: 1 }
        title:
          { type: string, example: "Tips Memaksimalkan Koneksi Internet Fiber" }
        content:
          {
            type: string,
            example: "Internet fiber optik adalah teknologi koneksi...",
          }
        image:
          { type: string, example: "https://cdn.example.com/article-fiber.jpg" }
        author:
          nullable: true
          type: object
          properties:
            name: { type: string, example: "Tim Nusanet" }
            employeeId: { type: string, example: "EMP-001" }
            photo: { type: string, nullable: true, example: "https://cdn.example.com/photo.jpg" }
        readingTime: { type: string, example: "3 menit baca" }
        isView: { type: boolean, example: false }
        category:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 1 }
            name: { type: string, example: "Tips & Trik" }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-02-10T08:00:00.000Z",
          }

    EducationVideo:
      type: object
      properties:
        id: { type: integer, example: 1 }
        title:
          { type: string, example: "Cara Menggunakan Dashboard Kawan Nusa" }
        url:
          { type: string, example: "https://www.youtube.com/watch?v=example" }
        thumbnail:
          { type: string, example: "https://cdn.example.com/video-thumb.jpg" }
        description:
          {
            type: string,
            example: "Tutorial lengkap menggunakan fitur-fitur dashboard Kawan Nusa",
          }
        author:
          nullable: true
          type: object
          properties:
            name: { type: string, example: "Tim Nusanet" }
            employeeId: { type: string, example: "EMP-001" }
            photo: { type: string, nullable: true, example: "https://cdn.example.com/photo.jpg" }
        isView: { type: boolean, example: true }
        category:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 2 }
            name: { type: string, example: "Tutorial" }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-02-15T08:00:00.000Z",
          }

    # --- Template Schemas ---
    Template:
      type: object
      properties:
        id: { type: integer, example: 1 }
        name: { type: string, example: "Banner Promo Lebaran 2024" }
        description:
          {
            type: string,
            nullable: true,
            example: "Template banner untuk promosi Hari Raya Lebaran",
          }
        thumbnail:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/template-thumb.jpg",
          }
        png:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/template.png",
          }
        jpg:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/template.jpg",
          }
        psd:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/template.psd",
          }
        mp4:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/template.mp4",
          }
        isActive: { type: boolean, example: true }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-01-20T08:00:00.000Z",
          }

    # --- Service Promotion Schemas ---
    ServicePromotion:
      type: object
      properties:
        id: { type: integer, example: 1 }
        title: { type: string, example: "Promo Double Speed Februari 2024" }
        description:
          {
            type: string,
            example: "Upgrade kecepatan internet Anda 2x lipat tanpa biaya tambahan",
          }
        image:
          { type: string, example: "https://cdn.example.com/promo-feb.jpg" }
        url:
          {
            type: string,
            nullable: true,
            example: "https://nusanet.id/promo/double-speed",
          }
        startPeriod: { type: string, format: date, example: "2024-02-01" }
        endPeriod: { type: string, format: date, example: "2024-02-29" }
        createdAt:
          {
            type: string,
            format: date-time,
            example: "2024-01-25T08:00:00.000Z",
          }
        service:
          type: object
          properties:
            code: { type: string, example: "SVC-FIBER-001" }
            name: { type: string, example: "Fiber Home 20 Mbps" }

    # --- Statistics Schemas ---
    Achievement:
      type: object
      properties:
        percentage: { type: integer, example: 12 }
        isUp: { type: boolean, example: true }

    StatisticCount:
      type: object
      properties:
        customer:
          type: object
          properties:
            value: { type: integer, example: 148 }
            achievement: { $ref: "#/components/schemas/Achievement" }
        customerService:
          type: object
          properties:
            value: { type: integer, example: 215 }
            achievement: { $ref: "#/components/schemas/Achievement" }
        point:
          type: object
          properties:
            value: { type: integer, example: 25500 }
            achievement: { $ref: "#/components/schemas/Achievement" }

    # --- Additional Schemas ---
    AdditionalItem:
      type: object
      properties:
        code: { type: string, example: "WARNET" }
        name: { type: string, example: "Warnet" }

    SearchResult:
      type: object
      properties:
        title: { type: string, example: "Warnet Jaya" }
        module: { type: string, example: "Customer" }
        route: { type: string, example: "/customer/CUST-0001" }

    # --- Feedback Schemas ---
    FeedbackItem:
      type: object
      properties:
        timestamp:
          { type: string, format: date-time, example: "2026-04-16T04:34:54.591Z" }
        userId: { type: string, example: "1" }
        name: { type: string, example: "Budi Santoso" }
        images:
          type: array
          items: { type: string, format: uri, example: "https://domain.com/uploads/feedback/feedback_1_1744123456789_0.jpg" }
        url: { type: string, format: uri, example: "https://nusa.net.id" }
        category: { type: string, example: "gallery" }
        message: { type: string, example: "Mengirim banyak gambar" }
        type: { type: string, example: "" }
        reply: { type: string, example: "" }

    StoreFeedbackRequest:
      type: object
      required: [message, type, "images[]"]
      properties:
        message: { type: string, example: "Pelayanan sangat memuaskan" }
        type: { type: string, example: "gallery" }
        url: { type: string, format: uri, example: "https://nusa.net.id" }
        "images[]":
          type: array
          minItems: 1
          maxItems: 5
          items: { type: string, format: binary }
          description: "1–5 image files (jpg, png, jpeg, etc)"

    # --- User List Schemas ---
    UserListItem:
      type: object
      properties:
        id: { type: integer, example: 1 }
        name: { type: string, example: "Budi Santoso" }
        photo:
          {
            type: string,
            nullable: true,
            example: "https://cdn.example.com/photo.jpg",
          }
        email:
          { type: string, format: email, nullable: true, example: "budi.santoso@nusanet.id" }
        phone: { type: string, nullable: true, example: "081234567890" }
        identityNumber: { type: integer, nullable: true, example: 3201234567890001 }
        taxNumber: { type: string, nullable: true, example: "01.234.567.8-901.000" }
        bank:
          type: object
          properties:
            holderName: { type: string, nullable: true, example: "Budi Santoso" }
            name: { type: string, nullable: true, example: "BCA" }
            number: { type: string, nullable: true, example: "1234567890" }
        status: { type: string, enum: [pending, active, inactive, reject, revision], nullable: true, example: active }
        lastReferanceDate: { type: string, format: date, nullable: true, example: "2024-03-15" }
        point: { type: number, example: 25500 }

    # --- Point Submission Schemas ---
    PointSubmission:
      type: object
      properties:
        id: { type: integer, example: 1 }
        type: { type: string, enum: [OTC, Bulanan], example: "OTC" }
        point: { type: number, example: 100 }
        price: { type: number, example: 10000 }
        nisData:
          type: object
          properties:
            custServId: { type: integer, example: 12345 }
            custId: { type: string, example: "CUST001" }
            accountName: { type: string, example: "PT Nusanet" }
            serviceCode: { type: string, example: "SVC001" }
            serviceName: { type: string, example: "Internet 50Mbps" }
            accountManager: { type: string, nullable: true, example: "John Doe" }
            salesEmployeeId: { type: string, nullable: true, example: "EMP-001" }
        status: { type: string, enum: [pending, approved, rejected], example: "pending" }
        scheduleId: { type: integer, nullable: true, example: null, description: "Set when auto-generated by a monthly schedule (null = created manually)" }
        isAuto: { type: boolean, example: false, description: "true when this submission was generated by a monthly schedule" }
        period: { type: string, format: date, nullable: true, example: null, description: "Month this submission grants points for (schedule-generated only)" }
        notes: { type: string, nullable: true, example: "Approved by admin" }
        user:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 5 }
            name: { type: string, example: "Budi Santoso" }
        createdBy:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 1 }
            name: { type: string, example: "Admin Nusanet" }
        approvedBy:
          nullable: true
          type: object
          properties:
            id: { type: integer, example: 2 }
            name: { type: string, example: "Manager Nusanet" }
        approvedAt: { type: string, format: date-time, nullable: true, example: "2024-06-01T08:00:00.000Z" }
        createdAt: { type: string, format: date-time, example: "2024-06-01T07:00:00.000Z" }

    CreatePointSubmissionRequest:
      type: object
      required: [userId, type, price, nisData]
      properties:
        userId: { type: integer, example: 5 }
        type: { type: string, enum: [OTC, Bulanan], example: "OTC" }
        price: { type: number, minimum: 0, example: 10000 }
        nisData:
          type: object
          required: [custServId, custId, accountName, serviceCode, serviceName]
          properties:
            custServId: { type: integer, example: 12345 }
            custId: { type: string, example: "CUST001" }
            accountName: { type: string, example: "PT Nusanet" }
            serviceCode: { type: string, example: "SVC001" }
            serviceName: { type: string, example: "Internet 50Mbps" }
            accountManager: { type: string, nullable: true, example: "John Doe" }
            salesEmployeeId: { type: string, nullable: true, example: "EMP-001" }

    ApprovePointSubmissionRequest:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          items: { type: integer }
          minItems: 1
          example: [1, 2, 3]
        notes: { type: string, example: "Approved batch" }

    NisAccountResult:
      type: object
      properties:
        custServId: { type: integer, example: 12345 }
        custId: { type: string, example: "CUST001" }
        accountName: { type: string, example: "PT Nusanet" }
        serviceCode: { type: string, example: "SVC001" }
        serviceName: { type: string, example: "Internet 50Mbps" }
        accountManager: { type: string, nullable: true, example: "John Doe" }
        salesEmployeeId: { type: string, nullable: true, example: "EMP-001" }

    # --- Role Schemas ---
    RoleListItem:
      type: object
      properties:
        id: { type: integer, example: 1 }
        name: { type: string, example: "Super Admin" }
        description: { type: string, nullable: true, example: "Full access to all features" }
        color: { type: string, nullable: true, example: "#FF5733" }
        permissions:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
          example:
            dashboard: ["L"]
            user: ["L", "T", "E", "H"]
            catalog: ["L", "T"]
        isDefault: { type: boolean, example: false }
        employeeCount: { type: integer, example: 5 }
        createdAt: { type: string, format: date-time, example: "2024-06-01T08:00:00.000Z" }
        updatedAt: { type: string, format: date-time, example: "2024-06-01T08:00:00.000Z" }

    RoleDetail:
      allOf:
        - $ref: "#/components/schemas/RoleListItem"
        - type: object
          properties:
            employees:
              type: array
              items:
                type: object
                properties:
                  id: { type: integer, example: 1 }
                  name: { type: string, example: "Admin Nusanet" }
                  email: { type: string, example: "admin@nusanet.id" }
                  photo: { type: string, nullable: true, example: "https://cdn.example.com/photo.jpg" }

    CreateRoleRequest:
      type: object
      required: [name, permissions]
      properties:
        name: { type: string, example: "Editor" }
        description: { type: string, example: "Can edit content" }
        color: { type: string, example: "#00FF00" }
        permissions:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
          example:
            education: ["L", "T", "E"]
            catalog: ["L"]
        employeeIds:
          type: array
          items: { type: integer }
          example: [1, 2, 3]

    UpdateRoleRequest:
      type: object
      properties:
        name: { type: string, example: "Editor Updated" }
        description: { type: string, example: "Updated description" }
        color: { type: string, example: "#0000FF" }
        permissions:
          type: object
          additionalProperties:
            type: array
            items: { type: string }
          example:
            education: ["L", "T", "E", "H"]
            catalog: ["L", "T"]
        employeeIds:
          type: array
          items: { type: integer }
          example: [1, 2]

    PermissionModule:
      type: object
      properties:
        key: { type: string, example: "dashboard" }
        label: { type: string, example: "Dashboard Overview" }
        group: { type: string, example: "Beranda" }
        actions:
          type: array
          items: { type: string }
          example: ["L"]

    RoleEmployee:
      type: object
      properties:
        id: { type: integer, example: 1 }
        name: { type: string, example: "Admin Nusanet" }
        email: { type: string, example: "admin@nusanet.id" }
        photo: { type: string, nullable: true, example: "https://cdn.example.com/photo.jpg" }
        roleId: { type: integer, nullable: true, example: 1 }

  parameters:
    PageParam:
      name: page
      in: query
      description: Page number (default 1)
      schema: { type: integer, default: 1, example: 1 }
    LimitParam:
      name: limit
      in: query
      description: Items per page (default 10)
      schema: { type: integer, default: 10, example: 10 }
    SearchParam:
      name: q
      in: query
      description: Search keyword
      schema: { type: string, example: "nusanet" }
    SortParam:
      name: sort
      in: query
      description: Sort field name
      schema: { type: string }
    OrderParam:
      name: order
      in: query
      description: Sort direction
      schema: { type: string, enum: [ASC, DESC], default: DESC }
    StartDateParam:
      name: startDate
      in: query
      description: Filter from date (YYYY-MM-DD)
      schema: { type: string, format: date, example: "2024-01-01" }
    EndDateParam:
      name: endDate
      in: query
      description: Filter to date (YYYY-MM-DD)
      schema: { type: string, format: date, example: "2024-12-31" }

  responses:
    UnauthorizedResponse:
      description: Unauthorized - missing or invalid token
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/BaseResponse"
            example:
              success: false
              statusCode: 401
              message: "Unauthorized"

    NotFoundResponse:
      description: Not Found
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/BaseResponse"
            example:
              success: false
              statusCode: 404
              message: "Resource not found"

    ValidationErrorResponse:
      description: Validation Error
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ValidationError" }

paths:
  # =============================================
  # AUTH ROUTES
  # =============================================
  /auth/register:
    post:
      summary: Register new partner account
      description: "Register a new partner account with personal info, bank details, and document uploads (KTP & bank book). Account will be created with status=null. Status becomes pending after completing onboarding."
      tags: [Auth]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema: { $ref: "#/components/schemas/RegisterRequest" }
      responses:
        "201":
          description: User registered successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthUser" }
              example:
                success: true
                statusCode: 201
                message: "User registered successfully"
                data:
                  id: 1
                  name: "Budi Santoso"
                  photo: null
                  email: "budi.santoso@nusanet.id"
                  phone: "081234567890"
                  status: null
                  role: "user"
        "400":
          description: Email or phone already in use
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Email already in use"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /auth/verify-email:
    get:
      summary: Verify email and auto-login
      description: "Verify the user's email using the token sent via email. Sets isVerified=true (status remains null until onboarding is completed) and returns auth tokens for auto-login."
      tags: [Auth]
      parameters:
        - name: token
          in: query
          required: true
          schema: { type: string }
          description: "Email verification token"
      responses:
        "200":
          description: Email verified and logged in successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Email verified successfully"
                data:
                  user:
                    id: 1
                    name: "John Doe"
                    photo: null
                    email: "john@example.com"
                    phone: "081234567890"
                    status: active
                    isBoarding: false
                    role: "user"
                  accessToken: "eyJhbGciOiJIUzI1NiIs..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIs..."
        "400":
          description: Invalid/expired token or already verified
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Invalid or expired verification token"

  /auth/resend-verification:
    post:
      summary: Resend email verification
      description: "Resend the verification email to an unverified user."
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email, example: "budi.santoso@nusanet.id" }
      responses:
        "200":
          description: Verification email sent successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Verification email sent successfully"
                data: null
        "400":
          description: Email not found or already verified
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Email already verified"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /auth/check-email-status:
    get:
      summary: Check email verification status
      description: "Lightweight check to see if an email needs verification. Does not send any email."
      tags: [Auth]
      parameters:
        - name: email
          in: query
          required: true
          schema: { type: string, format: email }
          description: "Email address to check"
      responses:
        "200":
          description: Email needs verification
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Success"
                data:
                  needsVerification: true
        "400":
          description: Email not found or already verified
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Email already verified"

  /auth/login:
    post:
      summary: Login to partner account
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
            example:
              identifier: "budi.santoso@nusanet.id"
              password: "secret123"
      responses:
        "200":
          description: Logged in successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
              example:
                success: true
                statusCode: 200
                message: "Logged in successfully"
                data:
                  user:
                    id: 1
                    name: "Budi Santoso"
                    photo: null
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    status: active
                    role: "user"
                  accessToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        "401":
          description: Invalid credentials
          content:
            application/json:
              example:
                success: false
                statusCode: 401
                message: "Invalid credentials"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /auth/google:
    post:
      summary: Login with Google OAuth (user)
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code: { type: string, example: "4/0AX4XfWh..." }
            example:
              code: "4/0AX4XfWh..."
      responses:
        "200":
          description: Logged in successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
              example:
                success: true
                statusCode: 200
                message: "Logged in successfully"
                data:
                  user:
                    id: 1
                    name: "Budi Santoso"
                    photo: null
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    status: active
                    role: "user"
                  accessToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        "400":
          description: User not registered
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "User not registered"

  /auth/admin/google:
    post:
      summary: Login with Google OAuth (admin/employee)
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code: { type: string, example: "4/0AX4XfWh..." }
            example:
              code: "4/0AX4XfWh..."
      responses:
        "200":
          description: Logged in successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
              example:
                success: true
                statusCode: 200
                message: "Logged in successfully"
                data:
                  user:
                    id: 10
                    name: "Admin User"
                    photo: null
                    email: "admin@nusanet.id"
                    phone: "081234567890"
                    status: active
                    role: "admin"
                  accessToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        "400":
          description: Employee not registered
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Employee not registered"

  /auth/forgot-password:
    post:
      summary: Request password reset link via email
      description: |
        Generates a new reset token stored in a separate `password_reset_tokens` table.
        Multiple tokens can exist per user (each forgot-password request creates a new one).
        Tokens expire after 10 hours.
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForgotPasswordRequest" }
            example:
              email: "budi.santoso@nusanet.id"
      responses:
        "200":
          description: Reset instructions sent to email
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Password reset instructions have been sent to your email"
                data: null
        "400":
          description: Email not found
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Email not found"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /auth/validate-reset-token:
    get:
      summary: Validate a password reset token
      description: |
        Validates the token from the `password_reset_tokens` table.
        Only the token is required — the user is resolved from the token's relation.
      tags: [Auth]
      parameters:
        - name: token
          in: query
          required: true
          description: Reset token received via email
          schema: { type: string }
          example: "a3f8c2d1e9b4..."
      responses:
        "200":
          description: Token is valid
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Token is valid"
                data: null
        "400":
          description: Invalid or expired token
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Invalid or expired reset token"

  /auth/reset-password:
    post:
      summary: Reset password using token from email
      description: |
        Validates the token from the `password_reset_tokens` table, updates the user's password,
        then deletes **all** reset tokens for that user.
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ResetPasswordRequest" }
            example:
              token: "a3f8c2d1e9b4..."
              newPassword: "newSecret123"
      responses:
        "200":
          description: Password reset successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Password has been successfully reset"
                data: null
        "400":
          description: Invalid or expired token
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Invalid or expired reset token"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /auth/refresh:
    post:
      summary: Refresh access token using refresh token
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RefreshTokenRequest" }
            example:
              refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      responses:
        "200":
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
              example:
                success: true
                statusCode: 200
                message: "Token refreshed successfully"
                data:
                  user:
                    id: 1
                    name: "Budi Santoso"
                    photo: null
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    status: active
                    role: "user"
                  accessToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.new..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.new..."
        "401":
          description: Invalid or expired refresh token
          content:
            application/json:
              example:
                success: false
                statusCode: 401
                message: "Invalid or expired refresh token"

  /auth/me:
    get:
      summary: Get authenticated user details
      tags: [Auth]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: User profile retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AuthUser" }
              example:
                success: true
                statusCode: 200
                message: "User profile retrieved successfully"
                data:
                  id: 1
                  name: "Budi Santoso"
                  photo: null
                  email: "budi.santoso@nusanet.id"
                  phone: "081234567890"
                  status: active
                  role: "user"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /auth/logout:
    post:
      summary: Logout (client should discard tokens)
      tags: [Auth]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Logged out successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Logged out successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /auth/otp/send:
    post:
      summary: Send OTP code for login
      description: "Sends a 6-digit OTP code to the user's email or phone. If identifier contains '@', sends via email. Otherwise, logs to console (phone not yet implemented)."
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [identifier]
              properties:
                identifier:
                  type: string
                  description: "Email address or phone number"
                  example: "user@example.com"
      responses:
        "200":
          description: OTP sent successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "OTP sent successfully"
                data:
                  type: "email"
        "400":
          description: User not found, not verified, or inactive
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "User not found"

  /auth/otp/verify:
    post:
      summary: Verify OTP code and login
      description: "Verifies the OTP code and returns auth tokens for auto-login. OTP codes expire after 5 minutes."
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [identifier, code]
              properties:
                identifier:
                  type: string
                  description: "Email address or phone number used to request OTP"
                  example: "user@example.com"
                code:
                  type: string
                  description: "6-digit OTP code"
                  example: "123456"
      responses:
        "200":
          description: OTP verified, logged in successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Logged in successfully"
                data:
                  user:
                    id: 1
                    name: "John Doe"
                    email: "user@example.com"
                    phone: "081234567890"
                    status: active
                    isBoarding: false
                    role: "user"
                  accessToken: "eyJhbGciOiJIUzI1NiIs..."
                  refreshToken: "eyJhbGciOiJIUzI1NiIs..."
        "400":
          description: Invalid or expired OTP code
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Invalid or expired OTP code"

  # =============================================
  # PROFILE ROUTES
  # =============================================
  /profile:
    get:
      summary: Get current user profile
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Profile retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/UserProfile" }
              example:
                success: true
                statusCode: 200
                message: "User profile retrieved successfully"
                data:
                  id: 1
                  firstName: "Budi"
                  lastName: "Santoso"
                  email: "budi.santoso@nusanet.id"
                  phone: "081234567890"
                  company: "PT Nusanet"
                  jobPosition: "Sales Manager"
                  birthDate: "1990-05-15"
                  birthPlace: "Surabaya"
                  address: "Jl. Raya Darmo No. 10, Surabaya"
                  companyAddress: "Jl. Ngagel Jaya No. 88, Surabaya"
                  photo: "https://cdn.example.com/photo.jpg"
                  identityPath: "https://cdn.example.com/identity/1_1234567890.jpg"
                  bankDetails:
                    holderName: "Budi Santoso"
                    name: "BCA"
                    number: "1234567890"
                    accountPath: "https://cdn.example.com/account/1_1234567890.jpg"
                  settings:
                    isSubscribe: true
                    isAutoWithdraw: false
                  createdAt: "2024-06-01T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /profile/account:
    put:
      summary: Update basic account information
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateAccountRequest" }
            example:
              firstName: "Budi"
              lastName: "Wijaya"
              email: "budi.wijaya@nusanet.id"
              phone: "089876543210"
              company: "PT Nusanet Indonesia"
              jobPosition: "Senior Sales Manager"
              birthDate: "1990-05-15"
              birthPlace: "Surabaya"
              address: "Jl. Raya Darmo No. 10, Surabaya"
              companyAddress: "Jl. Ngagel Jaya No. 88, Surabaya"
      responses:
        "200":
          description: Account updated successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Account updated successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /profile/bank:
    put:
      summary: Update bank account information
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBankRequest" }
            example:
              accountHolderName: "Budi Santoso"
              bankName: "Mandiri"
              accountNumber: "9876543210"
      responses:
        "200":
          description: Bank details updated successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Bank details updated successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /profile/preference:
    put:
      summary: Update notification and auto-withdraw preferences
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePreferenceRequest" }
            example:
              isSubscribe: true
              isAutoWithdraw: false
      responses:
        "200":
          description: Preferences updated successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Preferences updated successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /profile/password:
    put:
      summary: Change account password
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePasswordRequest" }
            example:
              oldPassword: "secret123"
              newPassword: "newSecret456"
      responses:
        "200":
          description: Password changed successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Password changed successfully"
                data: null
        "400":
          description: Old password is incorrect
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Old password is incorrect"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /profile/photo:
    post:
      summary: Upload profile photo
      description: "Accepted formats: JPG, PNG, GIF. Max size: 5MB."
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [photo]
              properties:
                photo:
                  type: string
                  format: binary
                  description: "Image file (JPG, PNG, GIF). Max 5MB."
      responses:
        "200":
          description: Photo uploaded successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Photo updated successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  # =============================================
  /profile/complete-boarding:
    post:
      summary: Mark boarding as complete
      description: |
        Sets isBoarding to true for the authenticated user after completing the onboarding flow.
        - First-time boarding: sets status from null to 'pending'.
        - Revision flow: sets status from 'revision' back to 'pending' (user re-submits data without setting password again).
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Boarding completed
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Boarding completed successfully"
                data:
                  id: 1
                  isBoarding: true
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /profile/documents:
    post:
      summary: Upload identity and bank account documents
      description: "Upload KTP (identity) and/or bank book (account) images. Both fields are optional — only provided files will be updated."
      tags: [Profile]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                identity:
                  type: string
                  format: binary
                  description: "KTP image file (JPG, PNG)"
                account:
                  type: string
                  format: binary
                  description: "Bank book image file (JPG, PNG)"
      responses:
        "200":
          description: Documents uploaded successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Documents updated successfully"
                data:
                  id: 1
                  identityPath: "https://minio.example.com/identity/1_123456.jpg"
                  accountPath: "https://minio.example.com/account/1_123456.jpg"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # POINT ROUTES
  # =============================================
  /point:
    get:
      summary: Get current point balance
      description: "Returns the current available point balance calculated using FIFO logic (expired points are excluded)."
      tags: [Point]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Point balance retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point balance retrieved successfully"
                data:
                  value: 25500
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # REWARD ROUTES
  # =============================================
  /point/reward:
    get:
      summary: List reward transactions (paginated)
      tags: [Point]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. createdAt, point, price)"
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "type[]"
          in: query
          description: "Filter by reward type (repeatable)"
          schema:
            type: array
            items: { type: string, enum: ["OTP", "Bulanan", "Cashback"] }
          style: form
          explode: true
          example: ["OTP", "Bulanan"]
      responses:
        "200":
          description: Rewards retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Rewards retrieved successfully"
                data:
                  - id: 101
                    price: 250000
                    expiredDate: "2025-03-01"
                    point: 500
                    remainingPoint: 500
                    isExpired: false
                    type: "OTP"
                    customerService:
                      id: 42
                      serviceCode: "SVC-FIBER-001"
                      activationDate: "2024-01-15"
                      startDate: "2024-01-15"
                      endDate: "2025-01-15"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                    customer:
                      id: "CUST-0001"
                      name: "Warnet Jaya"
                    createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 50
                  perPage: 10
                  currentPage: 1
                  lastPage: 5
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

    post:
      summary: Create a new reward (Internal use only)
      description: "Requires API Key authentication via `X-API-KEY` header. Used by internal systems to register reward points when a customer pays their bill."
      tags: [Point]
      security: [{ apiKeyAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePointRequest" }
            example:
              customerServiceId: 42
              userId: 5
              price: 250000
              point: 500
              type: "OTP"
      responses:
        "200":
          description: Reward created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Reward created successfully"
                data:
                  id: 101
                  price: 250000
                  expiredDate: "2025-03-01"
                  point: 500
                  remainingPoint: 500
                  isExpired: false
                  type: "OTP"
                  createdAt: "2024-03-01T09:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Customer service not found
          content:
            application/json:
              example:
                success: false
                statusCode: 404
                message: "Customer service not found"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  # =============================================
  # REDEMPTION ROUTES
  # =============================================
  /redemption/cash/list:
    get:
      summary: List all cash redemption transactions (admin)
      description: "Admin-only endpoint. Returns paginated list of all cash (withdraw) redemptions with user and bank details."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. createdAt, pointsUsed)"
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "status[]"
          in: query
          description: "Filter by redemption status (repeatable)"
          schema:
            type: array
            items:
              {
                type: string,
                enum: ["pending", "processing", "completed"],
              }
          style: form
          explode: true
      responses:
        "200":
          description: Cash redemptions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Cash redemptions retrieved successfully"
                data:
                  - id: 1
                    redempNo: "RED-20240301-0001"
                    pointsUsed: 1000
                    status: "pending"
                    notes: null
                    user:
                      id: 1
                      name: "Budi Santoso"
                      photo: "https://example.com/api/proxy?path=profile/1_1234567890.jpg"
                      email: "budi.santoso@nusanet.id"
                      phone: "081234567890"
                      identityNumber: 3201234567890001
                      taxNumber: "01.234.567.8-901.000"
                    withdrawDetails:
                      bankName: "BCA"
                      accountNumber: "1234567890"
                      accountHolderName: "Budi Santoso"
                      payout: 95000
                      tax: 5000
                    createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 20
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /redemption/cash/list/{id}:
    put:
      summary: Mark cash redemption as completed (admin)
      description: "Admin-only endpoint. Marks a pending/processing cash redemption transaction as completed."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema:
            type: integer
      responses:
        "200":
          description: Redemption marked as completed successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Redemption marked as completed successfully"
                data:
                  id: 1
                  redempNo: "RED-20240301-0001"
                  pointsUsed: 1000
                  status: "completed"
                  notes: null
                  createdAt: "2024-03-01T09:00:00.000Z"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Redemption record not found

  /redemption/product/list:
    get:
      summary: List all product redemption transactions (admin)
      description: "Admin-only endpoint. Returns paginated list of all product redemptions with user, catalog, and shipping details."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. createdAt, pointsUsed)"
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "status[]"
          in: query
          description: "Filter by redemption status (repeatable)"
          schema:
            type: array
            items:
              {
                type: string,
                enum: ["pending", "processing", "completed"],
              }
          style: form
          explode: true
      responses:
        "200":
          description: Product redemptions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Product redemptions retrieved successfully"
                data:
                  - id: 3
                    redempNo: "RED-20240305-0001"
                    pointsUsed: 500
                    status: "pending"
                    notes: "Tolong dikirim pagi hari"
                    user:
                      id: 1
                      name: "Budi Santoso"
                      photo: "https://example.com/api/proxy?path=profile/1_1234567890.jpg"
                      email: "budi.santoso@nusanet.id"
                      phone: "081234567890"
                    productDetails:
                      catalog:
                        id: 7
                        name: "Mug Kawan Nusa"
                        image: "https://cdn.example.com/mug.jpg"
                        category:
                          id: 3
                          name: "Merchandise"
                      name: "Budi Santoso"
                      email: "budi.santoso@nusanet.id"
                      phone: "081234567890"
                      address: "Jl. Raya No.10, Surabaya 60111"
                      shipping: null
                    createdAt: "2024-03-05T09:00:00.000Z"
                meta:
                  total: 15
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /redemption/product/list/{id}:
    post:
      summary: Process product redemption with shipping info (admin)
      description: "Admin-only endpoint. Changes product redemption status from pending to processing and creates a shipping record in redemption_product_shippings table. Requires shipper and tracking number."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [shipper, trackingNumber]
              properties:
                shipper:
                  type: string
                  enum: ["jne", "jnt", "sicepat", "ninja", "wahana", "pos", "others"]
                  example: "jne"
                trackingNumber:
                  type: string
                  example: "JNE001234567890"
      responses:
        "200":
          description: Product redemption is now processing
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Product redemption is now processing"
                data:
                  id: 3
                  redempNo: "RED-20240305-0001"
                  pointsUsed: 500
                  type: "product"
                  status: "processing"
                  notes: "Tolong dikirim pagi hari"
                  withdrawDetails: null
                  voucherDetails: null
                  productDetails:
                    catalog:
                      id: 7
                      name: "Mug Kawan Nusa"
                      image: "https://cdn.example.com/mug.jpg"
                      category:
                        id: 3
                        name: "Merchandise"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    address: "Jl. Raya No.10, Surabaya 60111"
                    shipping:
                      id: 1
                      shipper: "jne"
                      trackingNumber: "JNE001234567890"
                      shippedAt: "2024-03-06T10:00:00.000Z"
                  createdAt: "2024-03-05T09:00:00.000Z"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Redemption record not found
        "422":
          $ref: "#/components/responses/ValidationResponse"

    put:
      summary: Mark product redemption as completed (admin)
      description: "Admin-only endpoint. Marks a processing product redemption as completed. Only redemptions with status 'processing' can be completed."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema:
            type: integer
      responses:
        "200":
          description: Product redemption marked as completed successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Product redemption marked as completed successfully"
                data:
                  id: 3
                  redempNo: "RED-20240305-0001"
                  pointsUsed: 500
                  type: "product"
                  status: "completed"
                  notes: "Tolong dikirim pagi hari"
                  withdrawDetails: null
                  voucherDetails: null
                  productDetails:
                    catalog:
                      id: 7
                      name: "Mug Kawan Nusa"
                      image: "https://cdn.example.com/mug.jpg"
                      category:
                        id: 3
                        name: "Merchandise"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    address: "Jl. Raya No.10, Surabaya 60111"
                    shipping:
                      id: 1
                      shipper: "jne"
                      trackingNumber: "JNE001234567890"
                      shippedAt: "2024-03-06T10:00:00.000Z"
                  createdAt: "2024-03-05T09:00:00.000Z"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Redemption record not found

  /redemption/voucher/list:
    get:
      summary: List all voucher redemption transactions (admin)
      description: "Admin-only endpoint. Returns paginated list of all voucher redemptions with user, catalog, and voucher detail information."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. createdAt, pointsUsed)"
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "status[]"
          in: query
          description: "Filter by redemption status (repeatable)"
          schema:
            type: array
            items:
              {
                type: string,
                enum: ["pending", "processing", "completed"],
              }
          style: form
          explode: true
      responses:
        "200":
          description: Voucher redemptions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Voucher redemptions retrieved successfully"
                data:
                  - id: 2
                    redempNo: "RED-20240303-0001"
                    pointsUsed: 500
                    status: "pending"
                    notes: "Voucher untuk hadiah"
                    user:
                      id: 1
                      name: "Budi Santoso"
                      photo: "https://example.com/api/proxy?path=profile/1_1234567890.jpg"
                      email: "budi.santoso@nusanet.id"
                      phone: "081234567890"
                    voucherDetails:
                      catalog:
                        id: 5
                        name: "Voucher Tokopedia 50K"
                        image: "https://cdn.example.com/voucher.jpg"
                        category:
                          id: 2
                          name: "E-Commerce"
                      name: "Budi Santoso"
                      email: "budi.santoso@nusanet.id"
                      detail: null
                    createdAt: "2024-03-03T09:00:00.000Z"
                meta:
                  total: 12
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /redemption/voucher/list/{id}:
    post:
      summary: Process voucher redemption with voucher detail (admin)
      description: "Admin-only endpoint. Changes voucher redemption status from pending to processing and creates a voucher detail record in redemption_voucher_details table. Requires voucher code and optional expired date."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  example: "VCHRTOKO50K-ABCDE"
                expiredDate:
                  type: string
                  format: date
                  example: "2024-12-31"
                  description: "Optional expiry date for the voucher"
      responses:
        "200":
          description: Voucher redemption is now processing
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Voucher redemption is now processing"
                data:
                  id: 2
                  redempNo: "RED-20240303-0001"
                  pointsUsed: 500
                  type: "voucher"
                  status: "processing"
                  notes: "Voucher untuk hadiah"
                  withdrawDetails: null
                  voucherDetails:
                    catalog:
                      id: 5
                      name: "Voucher Tokopedia 50K"
                      image: "https://cdn.example.com/voucher.jpg"
                      category:
                        id: 2
                        name: "E-Commerce"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    detail:
                      id: 1
                      code: "VCHRTOKO50K-ABCDE"
                      expiredDate: "2024-12-31T00:00:00.000Z"
                  productDetails: null
                  createdAt: "2024-03-03T09:00:00.000Z"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Redemption record not found
        "422":
          $ref: "#/components/responses/ValidationResponse"

    put:
      summary: Mark voucher redemption as completed (admin)
      description: "Admin-only endpoint. Marks a processing voucher redemption as completed. Only redemptions with status 'processing' can be completed."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema:
            type: integer
      responses:
        "200":
          description: Voucher redemption marked as completed successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Voucher redemption marked as completed successfully"
                data:
                  id: 2
                  redempNo: "RED-20240303-0001"
                  pointsUsed: 500
                  type: "voucher"
                  status: "completed"
                  notes: "Voucher untuk hadiah"
                  withdrawDetails: null
                  voucherDetails:
                    catalog:
                      id: 5
                      name: "Voucher Tokopedia 50K"
                      image: "https://cdn.example.com/voucher.jpg"
                      category:
                        id: 2
                        name: "E-Commerce"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    detail:
                      id: 1
                      code: "VCHRTOKO50K-ABCDE"
                      expiredDate: "2024-12-31T00:00:00.000Z"
                  productDetails: null
                  createdAt: "2024-03-03T09:00:00.000Z"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Redemption record not found

  /redemption:
    get:
      summary: List redemption transactions (paginated)
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. createdAt, pointsUsed)"
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "status[]"
          in: query
          description: "Filter by redemption status (repeatable)"
          schema:
            type: array
            items:
              {
                type: string,
                enum: ["pending", "processing", "completed", "cancelled"],
              }
          style: form
          explode: true
        - name: "type[]"
          in: query
          description: "Filter by redemption type (repeatable)"
          schema:
            type: array
            items: { type: string, enum: ["cash", "voucher", "product"] }
          style: form
          explode: true
      responses:
        "200":
          description: Redemptions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Redemptions retrieved successfully"
                data:
                  - id: 1
                    redempNo: "RED-20240301-0001"
                    pointsUsed: 1000
                    type: "cash"
                    status: "pending"
                    notes: null
                    withdrawDetails:
                      bankName: "BCA"
                      accountNumber: "1234567890"
                      accountHolderName: "Budi Santoso"
                      payout: 95000
                      tax: 5000
                    voucherDetails: null
                    productDetails: null
                    createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 20
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /redemption/cash:
    post:
      summary: Create a cash (withdraw) redemption request
      description: "Converts points to cash payout. Points are deducted using FIFO logic. Tax is automatically calculated."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCashRedemptionRequest" }
            example:
              pointsUsed: 1000
              notes: "Mohon segera diproses"
      responses:
        "200":
          description: Cash redemption created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Cash redemption created successfully"
                data:
                  id: 1
                  redempNo: "RED-20240301-0001"
                  pointsUsed: 1000
                  type: "cash"
                  status: "pending"
                  notes: "Mohon segera diproses"
                  withdrawDetails:
                    bankName: "BCA"
                    accountNumber: "1234567890"
                    accountHolderName: "Budi Santoso"
                    payout: 95000
                    tax: 5000
                  voucherDetails: null
                  productDetails: null
                  createdAt: "2024-03-01T09:00:00.000Z"
        "400":
          description: Insufficient point balance
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Insufficient point balance. Available: 800, Required: 1000"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /redemption/voucher:
    post:
      summary: Create a voucher redemption request
      description: "Redeems points for a catalog voucher. Point cost is taken from the catalog item's point value."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/CreateRedemptionVoucherRequest" }
            example:
              catalogId: 5
              notes: "Voucher untuk hadiah"
      responses:
        "200":
          description: Voucher redemption created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Voucher redemption created successfully"
                data:
                  id: 2
                  redempNo: "RED-20240301-0002"
                  pointsUsed: 500
                  type: "voucher"
                  status: "pending"
                  notes: "Voucher untuk hadiah"
                  withdrawDetails: null
                  voucherDetails:
                    catalog:
                      id: 5
                      name: "Voucher Tokopedia 50K"
                      image: "https://cdn.example.com/voucher.jpg"
                      category:
                        id: 2
                        name: "E-Commerce"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    detail: null
                  productDetails: null
                  createdAt: "2024-03-01T09:15:00.000Z"
        "400":
          description: Insufficient points or catalog not found
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Insufficient point balance. Available: 400, Required: 500"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /redemption/product:
    post:
      summary: Create a physical product redemption request
      description: "Redeems points for a physical product from the catalog. Shipping address is required."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/CreateRedemptionProductRequest" }
            example:
              catalogId: 7
              address: "Jl. Raya No.10 RT 01/02, Kel. Tegalsari, Kec. Tegalsari, Surabaya 60111"
              notes: "Tolong dikirim pagi hari"
      responses:
        "200":
          description: Product redemption created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Product redemption created successfully"
                data:
                  id: 3
                  redempNo: "RED-20240301-0003"
                  pointsUsed: 750
                  type: "product"
                  status: "pending"
                  notes: "Tolong dikirim pagi hari"
                  withdrawDetails: null
                  voucherDetails: null
                  productDetails:
                    catalog:
                      id: 7
                      name: "Mug Kawan Nusa"
                      image: "https://cdn.example.com/mug.jpg"
                      category:
                        id: 3
                        name: "Merchandise"
                    name: "Budi Santoso"
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    address: "Jl. Raya No.10 RT 01/02, Kel. Tegalsari, Kec. Tegalsari, Surabaya 60111"
                    shipping: null
                  createdAt: "2024-03-01T09:30:00.000Z"
        "400":
          description: Insufficient points or catalog not found
          content:
            application/json:
              example:
                success: false
                statusCode: 400
                message: "Catalog item not found"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /redemption/{id}:
    get:
      summary: Get redemption detail by ID
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Redemption details retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Redemption" }
              example:
                success: true
                statusCode: 200
                message: "Redemption details retrieved successfully"
                data:
                  id: 1
                  redempNo: "RED-20240301-0001"
                  pointsUsed: 1000
                  type: "cash"
                  status: "completed"
                  notes: null
                  withdrawDetails:
                    bankName: "BCA"
                    accountNumber: "1234567890"
                    accountHolderName: "Budi Santoso"
                    payout: 95000
                    tax: 5000
                  voucherDetails: null
                  productDetails: null
                  createdAt: "2024-03-01T09:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /redemption/{id}/status-histories:
    get:
      summary: Get redemption status history timeline (admin)
      description: "Ordered list of status transitions for a redemption. Admin only; requires permission redemption.cash (L)."
      tags: [Redemption]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Redemption ID
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Status histories retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Status histories retrieved successfully"
                data:
                  - id: 1
                    fromStatus: null
                    toStatus: "pending"
                    note: null
                    changedBy: { id: 5, name: "Admin Nusanet" }
                    createdAt: "2024-03-01T09:00:00.000Z"
                  - id: 2
                    fromStatus: "pending"
                    toStatus: "completed"
                    note: "Transfer selesai"
                    changedBy: { id: 5, name: "Admin Nusanet" }
                    createdAt: "2024-03-01T10:15:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  # =============================================
  # CUSTOMER ROUTES
  # =============================================
  /customer:
    get:
      summary: List all customers (paginated)
      tags: [Customer]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - name: q
          in: query
          description: "Search by customer ID, name, or company"
          schema: { type: string, example: "Warnet" }
        - name: sort
          in: query
          description: "Sort field (id, name, company, registrationDate, isActive, createdAt, updatedAt)"
          schema: { type: string, default: "registrationDate" }
        - $ref: "#/components/parameters/OrderParam"
        - name: isActive
          in: query
          description: "Filter by active status (1 = active, 0 = inactive)"
          schema: { type: string, enum: ["0", "1"] }
        - name: "type[]"
          in: query
          description: "Filter by customer type (repeatable)"
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          example: ["Warnet", "Hotel"]
        - name: "serviceCode[]"
          in: query
          description: "Filter by service code (repeatable)"
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          example: ["SVC-FIBER-001"]
      responses:
        "200":
          description: Customer list retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Customer list retrieved successfully"
                data:
                  - id: "CUST-0001"
                    name: "Warnet Jaya"
                    company: "CV Jaya Abadi"
                    type: "Warnet"
                    registrationDate: "2024-01-15"
                    isActive: true
                    phones:
                      - id: 1
                        phone: "081234567890"
                        label: "Primary"
                    emails:
                      - id: 1
                        email: "warnet.jaya@example.com"
                        label: "Primary"
                meta:
                  total: 148
                  perPage: 10
                  currentPage: 1
                  lastPage: 15
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /customer/{id}:
    get:
      summary: Get customer detail by ID
      tags: [Customer]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Customer ID
          schema: { type: string }
          example: "CUST-0001"
      responses:
        "200":
          description: Customer retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CustomerDetail" }
              example:
                success: true
                statusCode: 200
                message: "Customer retrieved successfully"
                data:
                  id: "CUST-0001"
                  name: "Warnet Jaya"
                  company: "CV Jaya Abadi"
                  type: "Warnet"
                  registrationDate: "2024-01-15"
                  isActive: true
                  phones:
                    - id: 1
                      phone: "081234567890"
                      label: "Primary"
                  emails:
                    - id: 1
                      email: "warnet.jaya@example.com"
                      label: "Primary"
                  totalCustomerServices: 3
                  latestCustomerService:
                    id: 42
                    customerId: "CUST-0001"
                    serviceCode: "SVC-FIBER-001"
                    status: "AC"
                    activationDate: "2024-01-15"
                    referenceDate: "2024-03-01"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                      type: "Internet"
                    totalPoint: 1500
                    latestPoint:
                      point: 500
                      type: "OTP"
                      createdAt: "2024-03-01T09:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /customer/{id}/service:
    get:
      summary: Get services subscribed by a specific customer (paginated)
      tags: [Customer]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Customer ID
          schema: { type: string }
          example: "CUST-0001"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. referenceDate, registrationDate, activationDate, totalPoint, latestPoint.point)"
          schema: { type: string, default: "referenceDate" }
        - $ref: "#/components/parameters/OrderParam"
        - name: startRegistration
          in: query
          description: Filter by registration date from (YYYY-MM-DD)
          schema: { type: string, format: date, example: "2024-01-01" }
        - name: endRegistration
          in: query
          description: Filter by registration date to (YYYY-MM-DD)
          schema: { type: string, format: date, example: "2024-12-31" }
        - name: startActivation
          in: query
          description: Filter by activation date from (YYYY-MM-DD)
          schema: { type: string, format: date, example: "2024-01-01" }
        - name: endActivation
          in: query
          description: Filter by activation date to (YYYY-MM-DD)
          schema: { type: string, format: date, example: "2024-12-31" }
        - name: "status[]"
          in: query
          description: "Filter by service status (AC, NA, BL, FR). Repeatable."
          schema:
            type: array
            items: { type: string, enum: ["AC", "NA", "BL", "FR"] }
          style: form
          explode: true
      responses:
        "200":
          description: Customer services retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Customer services retrieved successfully"
                data:
                  - id: 42
                    customerId: "CUST-0001"
                    serviceCode: "SVC-FIBER-001"
                    registrationDate: "2024-01-10"
                    activationDate: "2024-01-15"
                    address: "Jl. Raya No.10, Surabaya"
                    startDate: "2024-01-15"
                    endDate: "2025-01-15"
                    status: "AC"
                    referenceDate: "2024-03-01"
                    sales:
                      id: 10
                      name: "Andi"
                      employeeId: "EMP-001"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                      type: "Internet"
                    totalPoint: 1500
                    latestPoint:
                      point: 500
                      type: "OTP"
                      createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 3
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 3
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /customer/{id}/point:
    get:
      summary: Get reward transactions for a specific customer (paginated)
      tags: [Customer]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Customer ID
          schema: { type: string }
          example: "CUST-0001"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          schema: { type: string, default: "createdAt" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "type[]"
          in: query
          description: "Filter by reward type (repeatable)"
          schema:
            type: array
            items: { type: string, enum: ["OTP", "Bulanan", "Cashback"] }
          style: form
          explode: true
      responses:
        "200":
          description: Customer rewards retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Customer rewards retrieved successfully"
                data:
                  - id: 101
                    price: 250000
                    expiredDate: "2025-03-01"
                    point: 500
                    remainingPoint: 500
                    isExpired: false
                    type: "OTP"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                    customer:
                      id: "CUST-0001"
                      name: "Warnet Jaya"
                    createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 8
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 8
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # SERVICE ROUTES
  # =============================================
  /service:
    get:
      summary: List all services (paginated)
      tags: [Service]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (name, code, totalCustomerServices, lastReferanceDate, totalPoint)"
          schema: { type: string, default: "name" }
        - name: order
          in: query
          schema: { type: string, enum: [ASC, DESC], default: ASC }
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: isActive
          in: query
          description: Filter by active status (1 or 0)
          schema: { type: string, enum: ["0", "1"] }
        - name: category
          in: query
          description: "Filter by service category"
          schema:
            {
              type: string,
              enum: ["Access Business", "Access Home", "Digital Business"],
            }
      responses:
        "200":
          description: Service list retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Service list retrieved successfully"
                data:
                  - id: 1
                    code: "SVC-FIBER-001"
                    name: "Fiber Home 20 Mbps"
                    description: "Internet fiber optic untuk rumahan"
                    type: "Internet"
                    isActive: true
                    price: 250000
                    unit: "Monthly"
                    category: "Access Home"
                    features: ["Speed 20 Mbps", "Unlimited Quota"]
                    url: "https://nusanet.id/products/fiber-home-20"
                    lastReferanceDate: "2024-03-15"
                    totalCustomerServices: 25
                    totalPoint: 12500
                meta:
                  total: 15
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /service/{code}:
    get:
      summary: Get service detail by code
      tags: [Service]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: code
          in: path
          required: true
          description: Service code
          schema: { type: string }
          example: "SVC-FIBER-001"
      responses:
        "200":
          description: Service retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Service retrieved successfully"
                data:
                  id: 1
                  code: "SVC-FIBER-001"
                  name: "Fiber Home 20 Mbps"
                  description: "Internet fiber optic untuk rumahan, kecepatan 20 Mbps"
                  type: "Internet"
                  isActive: true
                  price: 250000
                  unit: "Monthly"
                  category: "Access Home"
                  features: ["Speed 20 Mbps", "Unlimited Quota", "24/7 Support"]
                  url: "https://nusanet.id/products/fiber-home-20"
                  lastReferanceDate: "2024-03-15"
                  totalCustomerServices: 25
                  totalPoint: 12500
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /service/{code}/customer:
    get:
      summary: Get customers subscribed to a specific service (paginated)
      tags: [Service]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: code
          in: path
          required: true
          description: Service code
          schema: { type: string }
          example: "SVC-FIBER-001"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          schema: { type: string, default: "referenceDate" }
        - $ref: "#/components/parameters/OrderParam"
        - name: startRegistration
          in: query
          schema: { type: string, format: date, example: "2024-01-01" }
        - name: endRegistration
          in: query
          schema: { type: string, format: date, example: "2024-12-31" }
        - name: startActivation
          in: query
          schema: { type: string, format: date, example: "2024-01-01" }
        - name: endActivation
          in: query
          schema: { type: string, format: date, example: "2024-12-31" }
        - name: "status[]"
          in: query
          description: "Filter by status (AC, NA, BL, FR). Repeatable."
          schema:
            type: array
            items: { type: string, enum: ["AC", "NA", "BL", "FR"] }
          style: form
          explode: true
      responses:
        "200":
          description: Customer services retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Customer services retrieved successfully"
                data:
                  - id: 42
                    customerId: "CUST-0001"
                    serviceCode: "SVC-FIBER-001"
                    status: "AC"
                    referenceDate: "2024-03-01"
                    totalPoint: 1500
                meta:
                  total: 25
                  perPage: 10
                  currentPage: 1
                  lastPage: 3
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /service/promotion:
    get:
      summary: List service promotions (paginated)
      tags: [Service]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
      responses:
        "200":
          description: Service promotions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Service promotions retrieved successfully"
                data:
                  - id: 1
                    title: "Promo Double Speed Februari 2024"
                    description: "Upgrade kecepatan internet Anda 2x lipat tanpa biaya tambahan"
                    image: "https://cdn.example.com/promo-feb.jpg"
                    url: "https://nusanet.id/promo/double-speed"
                    startPeriod: "2024-02-01"
                    endPeriod: "2024-02-29"
                    createdAt: "2024-01-25T08:00:00.000Z"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                meta:
                  total: 5
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 5
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create a service promotion (admin)
      description: "Admin only; requires permission education (T). Multipart for the promo image."
      tags: [Service]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [title, serviceCode, startPeriod, endPeriod]
              properties:
                title: { type: string, example: "Promo Double Speed" }
                description: { type: string }
                serviceCode: { type: string, example: "SVC-FIBER-001" }
                url: { type: string, example: "https://nusanet.id/promo" }
                startPeriod: { type: string, format: date, example: "2024-02-01" }
                endPeriod: { type: string, format: date, example: "2024-02-29" }
                image: { type: string, format: binary }
      responses:
        "201":
          description: Service promotion created successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /service/promotion/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: Service promotion ID
        schema: { type: integer }
        example: 1
    get:
      summary: Get service promotion detail
      tags: [Service]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Service promotion retrieved successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update a service promotion (admin)
      description: "Admin only; requires permission education (E). Multipart; send image only to replace it."
      tags: [Service]
      security: [{ bearerAuth: [] }]
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                serviceCode: { type: string }
                url: { type: string }
                startPeriod: { type: string, format: date }
                endPeriod: { type: string, format: date }
                image: { type: string, format: binary }
      responses:
        "200":
          description: Service promotion updated successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete a service promotion (admin)
      description: "Admin only; requires permission education (H)."
      tags: [Service]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Service promotion deleted successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  # =============================================
  # CUSTOMER SERVICE ROUTES
  # =============================================
  /customer-service:
    get:
      summary: List all customer services (paginated)
      tags: [CustomerService]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field (e.g. referenceDate, registrationDate, totalPoint, latestPoint.point, latestPoint.type)"
          schema: { type: string, default: "referenceDate" }
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: "type[]"
          in: query
          description: "Filter by latest point type (repeatable)"
          schema:
            type: array
            items: { type: string, enum: ["OTP", "Bulanan", "Cashback"] }
          style: form
          explode: true
        - name: "serviceCode[]"
          in: query
          description: "Filter by service code (repeatable)"
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          example: ["SVC-FIBER-001"]
      responses:
        "200":
          description: Customer services retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Customer services retrieved successfully"
                data:
                  - id: 42
                    customerId: "CUST-0001"
                    serviceCode: "SVC-FIBER-001"
                    registrationDate: "2024-01-10"
                    activationDate: "2024-01-15"
                    address: "Jl. Raya No.10, Surabaya"
                    startDate: "2024-01-15"
                    endDate: "2025-01-15"
                    status: "AC"
                    referenceDate: "2024-03-01"
                    sales:
                      id: 10
                      name: "Andi"
                      employeeId: "EMP-001"
                    service:
                      code: "SVC-FIBER-001"
                      name: "Fiber Home 20 Mbps"
                      type: "Internet"
                    totalPoint: 1500
                    latestPoint:
                      point: 500
                      type: "OTP"
                      createdAt: "2024-03-01T09:00:00.000Z"
                meta:
                  total: 215
                  perPage: 10
                  currentPage: 1
                  lastPage: 22
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # TEMPLATE ROUTES
  # =============================================
  /template:
    get:
      summary: List shareable marketing templates (paginated)
      tags: [Template]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
      responses:
        "200":
          description: Templates retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Templates retrieved successfully"
                data:
                  - id: 1
                    name: "Banner Promo Lebaran 2024"
                    description: "Template banner untuk promosi Hari Raya Lebaran"
                    thumbnail: "https://cdn.example.com/template-thumb.jpg"
                    png: "https://cdn.example.com/template.png"
                    jpg: "https://cdn.example.com/template.jpg"
                    psd: "https://cdn.example.com/template.psd"
                    mp4: null
                    isActive: true
                    createdAt: "2024-01-20T08:00:00.000Z"
                meta:
                  total: 12
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create a marketing template (admin)
      description: "Admin only; requires permission education (T). Multipart with thumbnail + one or more asset files (png/jpg/psd/mp4)."
      tags: [Template]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, example: "Banner Promo Lebaran 2024" }
                description: { type: string }
                thumbnail: { type: string, format: binary }
                png: { type: string, format: binary }
                jpg: { type: string, format: binary }
                psd: { type: string, format: binary }
                mp4: { type: string, format: binary }
      responses:
        "201":
          description: Template created successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /template/{id}:
    get:
      summary: Get template detail by ID
      tags: [Template]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Template details retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Template details retrieved successfully"
                data:
                  id: 1
                  name: "Banner Promo Lebaran 2024"
                  description: "Template banner untuk promosi Hari Raya Lebaran"
                  thumbnail: "https://cdn.example.com/template-thumb.jpg"
                  png: "https://cdn.example.com/template.png"
                  jpg: "https://cdn.example.com/template.jpg"
                  psd: "https://cdn.example.com/template.psd"
                  mp4: null
                  isActive: true
                  createdAt: "2024-01-20T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update a marketing template (admin)
      description: "Admin only; requires permission education (E). Multipart; send a file field only to replace that asset."
      tags: [Template]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                thumbnail: { type: string, format: binary }
                png: { type: string, format: binary }
                jpg: { type: string, format: binary }
                psd: { type: string, format: binary }
                mp4: { type: string, format: binary }
      responses:
        "200":
          description: Template updated successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete a marketing template (admin)
      description: "Admin only; requires permission education (H)."
      tags: [Template]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Template deleted successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /template/{id}/download:
    get:
      summary: Download or redirect to a template file
      description: "Redirects (302) to the actual file URL for the requested type."
      tags: [Template]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
        - name: type
          in: query
          required: true
          description: File format to download
          schema: { type: string, enum: [png, jpg, psd, mp4] }
          example: "jpg"
      responses:
        "302":
          description: Redirect to the file source URL
        "404":
          description: Template or file type not found
          content:
            application/json:
              example:
                success: false
                statusCode: 404
                message: "File for type psd not found"

  # =============================================
  # CATALOG ROUTES
  # =============================================
  /catalog:
    get:
      summary: List all redemption catalog items (paginated)
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: categoryId
          in: query
          description: Filter by category ID
          schema: { type: integer, example: 2 }
      responses:
        "200":
          description: Catalog retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog retrieved successfully"
                data:
                  - id: 5
                    name: "Voucher Tokopedia 50K"
                    description: "Voucher belanja Tokopedia senilai Rp 50.000"
                    point: 500
                    image: "https://cdn.example.com/voucher-tokopedia.jpg"
                    type: "voucher"
                    stock: 47
                    stockUsed: 3
                    expiredDate: "2025-12-31"
                    category:
                      id: 2
                      name: "E-Commerce"
                    createdBy:
                      id: 1
                      name: "Admin Nusanet"
                      employeeId: "EMP-001"
                  - id: 7
                    name: "Mug Kawan Nusa"
                    description: "Mug eksklusif berlogo Kawan Nusa"
                    point: 750
                    image: "https://cdn.example.com/mug.jpg"
                    type: "product"
                    stock: 20
                    stockUsed: 5
                    expiredDate: null
                    category:
                      id: 3
                      name: "Merchandise"
                    createdBy:
                      id: 1
                      name: "Admin Nusanet"
                      employeeId: "EMP-001"
                meta:
                  total: 20
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create a new catalog item (admin only)
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [name, categoryId, type, stock]
              properties:
                name: { type: string, example: "Voucher Tokopedia 50K" }
                categoryId: { type: integer, example: 2 }
                type: { type: string, enum: ["product", "voucher"], example: "voucher" }
                point: { type: number, example: 500 }
                stock: { type: integer, example: 50 }
                description: { type: string, example: "Voucher belanja Tokopedia" }
                expiredDate: { type: string, format: date, example: "2025-12-31" }
                image: { type: string, format: binary }
      responses:
        "200":
          description: Catalog item created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog item created successfully"
                data:
                  id: 5
                  name: "Voucher Tokopedia 50K"
                  type: "voucher"
                  point: 500
                  stock: 50
                  stockUsed: 0
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /catalog/{id}:
    get:
      summary: Get catalog item detail by ID
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 5
      responses:
        "200":
          description: Catalog item retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog item retrieved successfully"
                data:
                  id: 5
                  name: "Voucher Tokopedia 50K"
                  description: "Voucher belanja Tokopedia senilai Rp 50.000"
                  point: 500
                  image: "https://cdn.example.com/voucher-tokopedia.jpg"
                  type: "voucher"
                  stock: 47
                  stockUsed: 3
                  expiredDate: "2025-12-31"
                  category:
                    id: 2
                    name: "E-Commerce"
                  createdBy:
                    id: 1
                    name: "Admin Nusanet"
                    employeeId: "EMP-001"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update a catalog item (admin only)
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 5
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name: { type: string, example: "Voucher Tokopedia 100K" }
                categoryId: { type: integer, example: 2 }
                type: { type: string, enum: ["product", "voucher"], example: "voucher" }
                point: { type: number, example: 1000 }
                stock: { type: integer, example: 30, description: "Sisa stok baru" }
                description: { type: string }
                expiredDate: { type: string, format: date }
                image: { type: string, format: binary }
      responses:
        "200":
          description: Catalog item updated successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog item updated successfully"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete a catalog item (admin only)
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 5
      responses:
        "200":
          description: Catalog item deleted successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog item deleted successfully"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /catalog/{id}/stock-history:
    get:
      summary: Get stock movement history for a catalog item (admin only)
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 5
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
      responses:
        "200":
          description: Stock history retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Stock history retrieved successfully"
                data:
                  - id: 1
                    catalogId: 5
                    type: "initial"
                    quantity: 50
                    stockBefore: 0
                    stockAfter: 50
                    notes: "Stok awal"
                    createdBy:
                      id: 1
                      name: "Admin Nusanet"
                    user: null
                    createdAt: "2024-06-01T08:00:00.000Z"
                  - id: 2
                    catalogId: 5
                    type: "redemption"
                    quantity: -1
                    stockBefore: 50
                    stockAfter: 49
                    notes: "Penukaran voucher oleh user #3"
                    createdBy: null
                    user:
                      id: 3
                      name: "John Doe"
                    createdAt: "2024-06-02T10:30:00.000Z"
                meta:
                  total: 2
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 2
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
          content:
            application/json:
              example:
                success: false
                statusCode: 403
                message: "Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /catalog/category:
    get:
      summary: List all catalog categories
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Catalog categories retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Catalog categories retrieved successfully"
                data:
                  - id: 1
                    name: "Pulsa & Token"
                  - id: 2
                    name: "E-Commerce"
                  - id: 3
                    name: "Merchandise"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create a catalog category (admin)
      description: "Admin only; requires permission catalog (T)."
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, example: "Voucher" }
      responses:
        "201":
          description: Catalog category created successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /catalog/category/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: Catalog category ID
        schema: { type: integer }
        example: 1
    put:
      summary: Update a catalog category (admin)
      description: "Admin only; requires permission catalog (E)."
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, example: "Voucher Belanja" }
      responses:
        "200":
          description: Catalog category updated successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete a catalog category (admin)
      description: "Admin only; requires permission catalog (H)."
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Catalog category deleted successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /catalog/upload:
    post:
      summary: Upload a catalog image (admin)
      description: "Admin only; requires permission catalog (T). Returns the stored object path/URL."
      tags: [Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
      responses:
        "200":
          description: Image uploaded successfully
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # EDUCATION ROUTES
  # =============================================
  /education/article:
    get:
      summary: List education articles (paginated)
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: categoryId
          in: query
          description: Filter by education category ID
          schema: { type: integer, example: 1 }
        - name: isView
          in: query
          description: Filter by viewed status (true = viewed, false = not viewed)
          schema: { type: string, enum: ["true", "false"] }
      responses:
        "200":
          description: Education articles retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Education articles retrieved successfully"
                data:
                  - id: 1
                    title: "Tips Memaksimalkan Koneksi Internet Fiber"
                    content: "Internet fiber optik adalah teknologi koneksi..."
                    image: "https://cdn.example.com/article-fiber.jpg"
                    author:
                      name: "Tim Nusanet"
                      employeeId: "EMP-001"
                      photo: "https://cdn.example.com/photo.jpg"
                    readingTime: "3 menit baca"
                    isView: false
                    category:
                      id: 1
                      name: "Tips & Trik"
                    createdAt: "2024-02-10T08:00:00.000Z"
                meta:
                  total: 30
                  perPage: 10
                  currentPage: 1
                  lastPage: 3
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create education article (admin)
      description: "Admin-only endpoint. Creates a new education article. Supports file upload for the thumbnail image."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              required: [categoryId, title, content]
              properties:
                categoryId:
                  type: integer
                  example: 1
                title:
                  type: string
                  example: "Tips Memaksimalkan Koneksi Internet Fiber"
                content:
                  type: string
                  example: "<p>Internet fiber optik adalah teknologi koneksi...</p>"
                image:
                  type: string
                  format: binary

      responses:
        "200":
          description: Article created successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /education/article/upload:
    post:
      summary: Upload image for rich editor (admin)
      description: "Admin-only endpoint. Uploads an image to MinIO and returns the public proxy URL to be inserted in the rich text editor."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: Image uploaded successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Image uploaded successfully"
                data:
                  url: "https://example.com/api/proxy?path=education/articles/editor/123456789.jpg"
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /education/article/{id}:
    get:
      summary: Get education article detail by ID (marks as viewed)
      description: "Retrieves the article and marks it as viewed by the authenticated user."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Education article retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Education article details retrieved successfully"
                data:
                  id: 1
                  title: "Tips Memaksimalkan Koneksi Internet Fiber"
                  content: "Internet fiber optik adalah teknologi koneksi yang menggunakan serat kaca..."
                  image: "https://cdn.example.com/article-fiber.jpg"
                  author:
                    name: "Tim Nusanet"
                    employeeId: "EMP-001"
                    photo: "https://cdn.example.com/photo.jpg"
                  readingTime: "3 menit baca"
                  isView: true
                  category:
                    id: 1
                    name: "Tips & Trik"
                  createdAt: "2024-02-10T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update education article (admin)
      description: "Admin-only endpoint. Updates an education article. Supports file upload for the thumbnail image."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                categoryId:
                  type: integer
                  example: 1
                title:
                  type: string
                  example: "Cara Mengatur Router Terbaru"
                content:
                  type: string
                  example: "<p>Langkah-langkah mengatur router baru...</p>"

                image:
                  type: string
                  format: binary
      responses:
        "200":
          description: Article updated successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete education article (admin)
      description: "Admin-only endpoint. Deletes an education article."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Article deleted successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /education/video:
    get:
      summary: List education videos (paginated)
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: categoryId
          in: query
          description: Filter by education category ID
          schema: { type: integer, example: 2 }
        - name: isView
          in: query
          description: Filter by viewed status (true = viewed, false = not viewed)
          schema: { type: string, enum: ["true", "false"] }
      responses:
        "200":
          description: Education videos retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Education videos retrieved successfully"
                data:
                  - id: 1
                    title: "Cara Menggunakan Dashboard Kawan Nusa"
                    url: "https://www.youtube.com/watch?v=example"
                    thumbnail: "https://cdn.example.com/video-thumb.jpg"
                    description: "Tutorial lengkap menggunakan fitur-fitur dashboard Kawan Nusa"
                    author:
                      name: "Tim Nusanet"
                      employeeId: "EMP-001"
                      photo: "https://cdn.example.com/photo.jpg"
                    isView: false
                    category:
                      id: 2
                      name: "Tutorial"
                    createdAt: "2024-02-15T08:00:00.000Z"
                meta:
                  total: 15
                  perPage: 10
                  currentPage: 1
                  lastPage: 2
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create education video (admin)
      description: "Admin-only endpoint. Creates a new education video. Supports optional file upload for the thumbnail image."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              required: [categoryId, title, url]
              properties:
                categoryId:
                  type: integer
                  example: 2
                title:
                  type: string
                  example: "Tutorial Dashboard"
                url:
                  type: string
                  example: "https://www.youtube.com/watch?v=example"
                description:
                  type: string
                  example: "Deskripsi video tutorial"

                thumbnail:
                  type: string
                  format: binary
      responses:
        "200":
          description: Video created successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /education/video/{id}:
    get:
      summary: Get education video detail by ID (marks as viewed)
      description: "Retrieves the video and marks it as viewed by the authenticated user."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Education video retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Education video details retrieved successfully"
                data:
                  id: 1
                  title: "Cara Menggunakan Dashboard Kawan Nusa"
                  url: "https://www.youtube.com/watch?v=example"
                  thumbnail: "https://cdn.example.com/video-thumb.jpg"
                  description: "Tutorial lengkap menggunakan fitur-fitur dashboard Kawan Nusa"
                  author:
                    name: "Tim Nusanet"
                    employeeId: "EMP-001"
                    photo: "https://cdn.example.com/photo.jpg"
                  isView: true
                  category:
                    id: 2
                    name: "Tutorial"
                  createdAt: "2024-02-15T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update education video (admin)
      description: "Admin-only endpoint. Updates an education video. Supports optional file upload for the thumbnail image."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                categoryId:
                  type: integer
                  example: 2
                title:
                  type: string
                  example: "Tutorial Dashboard Updated"
                url:
                  type: string
                  example: "https://www.youtube.com/watch?v=example"
                description:
                  type: string
                  example: "Deskripsi video tutorial terupdate"

                thumbnail:
                  type: string
                  format: binary
      responses:
        "200":
          description: Video updated successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete education video (admin)
      description: "Admin-only endpoint. Deletes an education video."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Video deleted successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /education/category:
    get:
      summary: List all education categories
      tags: [Education]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Education categories retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Education categories retrieved successfully"
                data:
                  - id: 1
                    name: "Tips & Trik"
                  - id: 2
                    name: "Tutorial"
                  - id: 3
                    name: "Berita"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
    post:
      summary: Create education category (admin)
      description: "Admin-only endpoint. Creates a new education category."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  example: "Tutorial"
      responses:
        "200":
          description: Category created successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /education/category/{id}:
    put:
      summary: Update education category (admin)
      description: "Admin-only endpoint. Updates an education category."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  example: "Tutorial Baru"
      responses:
        "200":
          description: Category updated successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    delete:
      summary: Delete education category (admin)
      description: "Admin-only endpoint. Deletes an education category."
      tags: [Education]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Category deleted successfully
        "400":
          $ref: "#/components/responses/BadRequestResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  # =============================================
  # STATISTIC ROUTES
  # =============================================
  /statistic/count:
    get:
      summary: Get dashboard summary counts with achievement comparison
      description: "Returns total customers, customer services, and available points with month-over-month percentage achievement."
      tags: [Statistic]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Statistic count retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Statistic count retrieved successfully"
                data:
                  customer:
                    value: 148
                    achievement:
                      percentage: 12
                      isUp: true
                  customerService:
                    value: 215
                    achievement:
                      percentage: 8
                      isUp: true
                  point:
                    value: 25500
                    achievement:
                      percentage: 5
                      isUp: false
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /statistic/point:
    get:
      summary: Get monthly point accumulation chart data for current year
      tags: [Statistic]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Monthly point statistics retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Monthly point statistics retrieved successfully"
                data:
                  - label: "Januari"
                    total: 2500
                  - label: "Februari"
                    total: 3100
                  - label: "Maret"
                    total: 2800
                  - label: "April"
                    total: 0
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /statistic/customer:
    get:
      summary: Get customer growth chart data
      description: "Returns customer growth statistics. Use `type=yearly` for monthly breakdown, or `type=monthly` for daily breakdown of current month."
      tags: [Statistic]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: type
          in: query
          description: "Chart granularity. `yearly` = per month for current year, `monthly` = per day for current month"
          schema:
            type: string
            enum: [yearly, monthly]
            default: yearly
      responses:
        "200":
          description: Customer statistics retrieved
          content:
            application/json:
              examples:
                yearly:
                  summary: Yearly view (per month)
                  value:
                    success: true
                    statusCode: 200
                    message: "Customer yearly statistics retrieved successfully"
                    data:
                      - label: "Januari"
                        count: 12
                      - label: "Februari"
                        count: 18
                      - label: "Maret"
                        count: 9
                monthly:
                  summary: Monthly view (per day)
                  value:
                    success: true
                    statusCode: 200
                    message: "Customer monthly statistics retrieved successfully"
                    data:
                      - label: "1 Maret"
                        count: 2
                      - label: "2 Maret"
                        count: 0
                      - label: "3 Maret"
                        count: 1
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /statistic/redemption-point:
    get:
      summary: Get redemption status breakdown for voucher and product redemptions
      tags: [Statistic]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Redemption reward statistics retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Redemption reward statistics retrieved successfully"
                data:
                  - status: "pending"
                    count: 5
                  - status: "processing"
                    count: 2
                  - status: "completed"
                    count: 30
                  - status: "cancelled"
                    count: 3
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /statistic/admin/summary:
    get:
      summary: Admin dashboard summary totals
      description: "Global totals for the admin dashboard. Admin only; requires permission dashboard (L)."
      tags: [Statistic]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Admin summary retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Admin summary retrieved successfully"
                data:
                  user: { value: 128 }
                  customer: { value: 540 }
                  customerService: { value: 712 }
                  point: { value: 1250000 }
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  # =============================================
  # ADDITIONAL / LOOKUP ROUTES
  # =============================================
  /additional/service:
    get:
      summary: List all available services (for dropdowns/filters)
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Additional services list retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Additional services list retrieved successfully"
                data:
                  - code: "SVC-FIBER-001"
                    name: "Fiber Home 20 Mbps"
                  - code: "SVC-FIBER-002"
                    name: "Fiber Home 50 Mbps"
                  - code: "SVC-BIZ-001"
                    name: "Business Fiber 100 Mbps"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /additional/customer-type:
    get:
      summary: List all customer types (for dropdowns/filters)
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Customer types retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Additional customer types list retrieved successfully"
                data:
                  - code: "AGRICULTURE_PLANTATION"
                    name: "Agriculture / Plantation"
                  - code: "AUTOMOTIVE"
                    name: "Automotive"
                  - code: "CAFE_RESTAURANT"
                    name: "Cafe / Restaurant"
                  - code: "EDUCATION_SCHOOL_UNIVERSITY"
                    name: "Education / School / University"
                  - code: "WARNET"
                    name: "Warnet"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /additional/customer-service-status:
    get:
      summary: List all customer service statuses (for dropdowns/filters)
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Customer service statuses retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Additional customer service statuses list retrieved successfully"
                data:
                  - code: "AC"
                    name: "Active"
                  - code: "NA"
                    name: "Non Active"
                  - code: "BL"
                    name: "Block"
                  - code: "FR"
                    name: "Free"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /additional/point-type:
    get:
      summary: List all reward point types (for dropdowns/filters)
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Reward point types retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Additional reward point types list retrieved successfully"
                data:
                  - code: "OTP"
                    name: "OTP"
                  - code: "BULANAN"
                    name: "Bulanan"
                  - code: "CASHBACK"
                    name: "Cashback"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /additional/service-category:
    get:
      summary: List all service categories (for dropdowns/filters)
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Service categories retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Additional service categories list retrieved successfully"
                data:
                  - code: "ACCESS_BUSINESS"
                    name: "Access Business"
                  - code: "ACCESS_HOME"
                    name: "Access Home"
                  - code: "DIGITAL_BUSINESS"
                    name: "Digital Business"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /additional/search:
    get:
      summary: Global search across modules (Customer, Service, Education)
      description: "Performs a UNION search across multiple modules. Customers are filtered based on the logged-in user."
      tags: [Additional]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SearchParam"
      responses:
        "200":
          description: Search results retrieved
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Search results retrieved successfully"
                data:
                  - title: "Warnet Jaya"
                    module: "customer"
                    route: "/customer/CUST-0001"
                  - title: "Fiber Home 20 Mbps"
                    module: "service"
                    route: "/service/SVC-FIBER-001"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /proxy:
    get:
      summary: Proxy a stored object from MinIO
      description: "Streams an object from object storage by its path. Public (no auth) — used to serve uploaded images/files."
      tags: [Additional]
      parameters:
        - name: path
          in: query
          required: true
          description: Object path/key in the bucket
          schema: { type: string }
          example: "catalog/1699999999_abc.png"
      responses:
        "200":
          description: The requested object stream
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        "400":
          description: Missing 'path' query parameter
          content:
            application/json:
              example:
                message: "Missing 'path' query parameter"

  # =============================================
  # USER ROUTES
  # =============================================
  /user:
    get:
      summary: List all users
      description: "Retrieve a paginated list of all users with their customer services count and available point balance. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - name: sort
          in: query
          description: "Sort field"
          schema: { type: string, default: createdAt, enum: [name, email, phone, identityNumber, taxNumber, status, lastReferanceDate, point, createdAt] }
        - $ref: "#/components/parameters/OrderParam"
        - name: status
          in: query
          description: "Filter by user status. Supports comma-separated values (e.g. active,inactive)"
          schema: { type: string, example: "active,inactive" }
      responses:
        "200":
          description: User list retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserListItem"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
              example:
                success: true
                statusCode: 200
                message: "User list retrieved successfully"
                data:
                  - id: 1
                    name: "Budi Santoso"
                    photo: "https://cdn.example.com/photo.jpg"
                    email: "budi.santoso@nusanet.id"
                    phone: "081234567890"
                    identityNumber: 3201234567890001
                    taxNumber: "01.234.567.8-901.000"
                    bank:
                      holderName: "Budi Santoso"
                      name: "BCA"
                      number: "1234567890"
                    status: active
                    lastReferanceDate: "2024-03-15"
                    point: 25500
                meta:
                  total: 50
                  perPage: 10
                  currentPage: 1
                  lastPage: 5
                  from: 1
                  to: 10
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"

  /user/{id}:
    get:
      summary: Get user detail
      description: "Retrieve a single user's full profile by ID. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer, example: 1 }
      responses:
        "200":
          description: User retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/UserProfile"
              example:
                success: true
                statusCode: 200
                message: "User retrieved successfully"
                data:
                  id: 1
                  firstName: "Budi"
                  lastName: "Santoso"
                  photo: "https://cdn.example.com/photo.jpg"
                  email: "budi.santoso@nusanet.id"
                  phone: "081234567890"
                  identityNumber: 3201234567890001
                  taxNumber: "01.234.567.8-901.000"
                  company: "PT Nusanet"
                  jobPosition: "Sales Manager"
                  bankDetails:
                    holderName: "Budi Santoso"
                    name: "BCA"
                    number: "1234567890"
                  settings:
                    isSubscribe: true
                    isAutoWithdraw: false
                  status: active
                  passwordUpdatedAt: "2025-01-15T10:30:00.000Z"
                  createdAt: "2024-06-01T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /user/{id}/status:
    patch:
      summary: Update user registration status
      description: |
        Approve, reject, or request revision for a user registration. Only users with status pending, revision, or reject can be updated. Admin only.
        A status change email notification is automatically sent to the user (fire-and-forget).
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status, note]
              properties:
                status:
                  type: string
                  enum: [active, reject, revision]
                  description: "Target status"
                note:
                  type: string
                  minLength: 1
                  maxLength: 110
                  description: "Approval/rejection note"
            example:
              status: active
              note: "Data lengkap dan valid."
      responses:
        "200":
          description: User status updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/UserProfile"
              example:
                success: true
                statusCode: 200
                message: "User status updated successfully"
                data:
                  id: 1
                  firstName: "Budi"
                  lastName: "Santoso"
                  status: active
                  statusNote: "Data lengkap dan valid."
                  statusUpdatedAt: "2025-01-15T10:30:00.000Z"
        "400":
          description: Invalid status transition
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 400
                message: "Cannot change status from 'active'"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
        "404":
          $ref: "#/components/responses/NotFoundResponse"
        "422":
          $ref: "#/components/responses/ValidationResponse"

  /user/{id}/status-histories:
    get:
      summary: Get a user's status change history (admin)
      description: "Ordered timeline of a referral partner's status transitions. Admin only; requires permission user (L)."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Status histories retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Status histories retrieved successfully"
                data:
                  - id: 1
                    fromStatus: null
                    toStatus: "pending"
                    note: null
                    changedBy: { id: 5, name: "Admin Nusanet" }
                    createdAt: "2024-03-01T09:00:00.000Z"
                  - id: 2
                    fromStatus: "pending"
                    toStatus: "active"
                    note: "Data lengkap dan valid"
                    changedBy: { id: 5, name: "Admin Nusanet" }
                    createdAt: "2024-03-02T10:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /user/{id}/services:
    get:
      summary: List customer services for a user
      description: "Retrieve paginated customer services belonging to a specific user. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer, example: 1 }
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/SortParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
      responses:
        "200":
          description: Customer services retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/CustomerServiceItem"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"

  /user/{id}/point:
    get:
      summary: List rewards for a user
      description: "Retrieve paginated reward point transactions for a specific user. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer, example: 1 }
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/SortParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
      responses:
        "200":
          description: Rewards retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Point"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"

  /user/{id}/redeem:
    get:
      summary: List redemptions for a user
      description: "Retrieve paginated redemption records for a specific user. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer, example: 1 }
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/SortParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
      responses:
        "200":
          description: Redemptions retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Redemption"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"

  /user/{id}/statistic:
    get:
      summary: Get statistics for a user
      description: "Retrieve combined statistics (count, monthly points, customer stats, redemption/point stats) for a specific user. Admin only."
      tags: [User]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: User ID
          schema: { type: integer, example: 1 }
        - name: type
          in: query
          description: "Customer stats type (yearly or monthly)"
          schema: { type: string, default: yearly, enum: [yearly, monthly] }
      responses:
        "200":
          description: Statistics retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          count:
                            $ref: "#/components/schemas/StatisticCount"
                          pointPerMonth:
                            type: array
                            items:
                              type: object
                              properties:
                                label: { type: string, example: "Januari" }
                                total: { type: number, example: 5000 }
                          customerStats:
                            type: array
                            items:
                              type: object
                              properties:
                                label: { type: string, example: "Januari" }
                                count: { type: integer, example: 10 }
                          redemptionPointStats:
                            type: array
                            items:
                              type: object
                              properties:
                                status: { type: string, example: "pending" }
                                count: { type: integer, example: 3 }
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin only
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 403
                message: "Forbidden access: admin only"

  # =============================================
  # FEEDBACK ROUTES
  # =============================================
  /notification:
    get:
      summary: List notifications for the current user (paginated)
      description: |
        Returns the user's own notifications plus broadcasts (userId=null), newest first.
        Each item carries `isRead` (per-user) and `isBroadcast`. **Role `user` only.**
        The client uses infinite scroll over these pages.
      tags: [Notification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
      responses:
        "200":
          description: Notifications retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Notifications retrieved successfully"
                data:
                  - id: 12
                    type: "point"
                    title: "Poin Baru"
                    message: "5.000 poin telah ditambahkan ke akun Anda."
                    link: "/point/activity/reward"
                    referenceId: null
                    isBroadcast: false
                    isRead: false
                    createdAt: "2026-07-18T09:00:00.000Z"
                  - id: 8
                    type: "content"
                    title: "Artikel Edukasi Baru"
                    message: "Artikel baru telah terbit: \"Tips Referral\"."
                    link: "/education/article/3"
                    referenceId: 3
                    isBroadcast: true
                    isRead: true
                    createdAt: "2026-07-17T09:00:00.000Z"
                meta: { total: 24, perPage: 10, currentPage: 1, lastPage: 3, from: 1, to: 10 }
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - user role only

  /notification/unread-count:
    get:
      summary: Unread notification count for the current user
      tags: [Notification]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Unread count retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Unread count retrieved successfully"
                data: { count: 3 }
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /notification/read-all:
    patch:
      summary: Mark all of the user's notifications as read
      tags: [Notification]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: All notifications marked as read
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "All notifications marked as read"
                data: { marked: 3 }
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /notification/{id}/read:
    patch:
      summary: Mark a single notification as read
      description: "Idempotent. 404 if the notification does not exist or is not visible to the user."
      tags: [Notification]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 12
      responses:
        "200":
          description: Notification marked as read
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /feedback:
    get:
      summary: Get feedback by logged-in user
      description: "Fetch all feedback entries from AppScript that belong to the currently authenticated user."
      tags: [Feedback]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Feedback retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Feedback retrieved successfully"
                data:
                  - timestamp: "2026-04-16T04:34:54.591Z"
                    userId: "1"
                    name: "Budi Santoso"
                    images:
                      - "https://domain.com/uploads/feedback/feedback_1_1744123456789_0.jpg"
                    url: "https://nusa.net.id"
                    category: "gallery"
                    message: "Pelayanan sangat memuaskan"
                    type: ""
                    reply: ""
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

    post:
      summary: Submit new feedback
      description: "Upload 1–5 images (stored locally), then send the feedback data to AppScript in fire-and-forget fashion. Returns immediately after images are saved."
      tags: [Feedback]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/StoreFeedbackRequest"
      responses:
        "201":
          description: Feedback submitted successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 201
                message: "Feedback submitted successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  # =============================================
  # POINT SUBMISSION ROUTES (Admin)
  # =============================================
  /point-submission:
    get:
      summary: List point submissions (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/SortParam"
        - $ref: "#/components/parameters/OrderParam"
        - $ref: "#/components/parameters/StartDateParam"
        - $ref: "#/components/parameters/EndDateParam"
        - name: status
          in: query
          schema: { type: string, enum: [pending, approved, rejected] }
        - name: type
          in: query
          schema: { type: string, enum: [OTC, Bulanan] }
      responses:
        "200":
          description: Point submissions retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point submissions retrieved successfully"
                data:
                  - id: 1
                    type: "OTC"
                    point: 100
                    price: 10000
                    nisData:
                      custServId: 12345
                      custId: "CUST001"
                      accountName: "PT Nusanet"
                      serviceCode: "SVC001"
                      serviceName: "Internet 50Mbps"
                      accountManager: "John Doe"
                      salesEmployeeId: "EMP-001"
                    status: "pending"
                    scheduleId: null
                    isAuto: false
                    period: null
                    notes: null
                    user:
                      id: 5
                      name: "Budi Santoso"
                    createdBy:
                      id: 1
                      name: "Admin Nusanet"
                    approvedBy: null
                    approvedAt: null
                    createdAt: "2024-06-01T07:00:00.000Z"
                meta:
                  total: 1
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 1
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
    post:
      summary: Create a new point submission (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePointSubmissionRequest"
      responses:
        "201":
          description: Point submission created successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 201
                message: "Point submission created successfully"
                data:
                  id: 1
                  type: "OTC"
                  point: 100
                  price: 10000
                  status: "pending"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /point-submission/check-account:
    get:
      summary: Check if a NIS account is already submitted (admin only)
      description: |
        Returns two flags:
        - `existsForUser`: true if this specific user already submitted this custServId (hard block)
        - `existsForOthers`: true if any user (including this one) already submitted this custServId (warning only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: custServId
          in: query
          required: true
          schema: { type: integer }
          example: 12345
        - name: userId
          in: query
          required: true
          schema: { type: integer }
          description: The referral user ID to check against
          example: 5
        - name: excludeId
          in: query
          schema: { type: integer }
          description: Exclude this submission ID from the check (for edit)
      responses:
        "200":
          description: Account check completed
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Account check completed"
                data:
                  existsForUser: false
                  existsForOthers: true
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required

  /point-submission/schedule:
    get:
      summary: List monthly recurring schedules (admin)
      description: |
        Monthly ("Bulanan") submissions create a schedule on first approval. A daily job then
        generates a new PENDING submission each month from every active schedule.
        Admin only; requires permission point-submission (L).
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - name: isActive
          in: query
          description: Filter by active state
          schema: { type: string, enum: ["true", "false"] }
      responses:
        "200":
          description: Schedules retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Schedules retrieved successfully"
                data:
                  - id: 1
                    price: 100000
                    point: 1000
                    anchorDay: 18
                    lastGeneratedPeriod: "2024-06-01"
                    isActive: true
                    nisData: { custServId: 12345, accountName: "PT Nusanet" }
                    user: { id: 5, name: "Budi Santoso" }
                    createdBy: { id: 2, name: "Admin Nusanet" }
                    stoppedBy: null
                    stoppedAt: null
                    createdAt: "2024-05-18T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"

  /point-submission/schedule/{id}/stop:
    patch:
      summary: Stop a monthly recurring schedule (admin)
      description: "Deactivates the schedule so no further monthly submissions are generated. Admin only; requires permission point-submission (E)."
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Schedule ID
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Schedule stopped successfully
        "400":
          description: Schedule is already stopped
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /point-submission/approve:
    post:
      summary: Approve one or more pending submissions (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovePointSubmissionRequest"
      responses:
        "200":
          description: Point submissions approved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point submissions approved successfully"
                data: null
        "400":
          description: Some submissions already processed
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /point-submission/{id}:
    get:
      summary: Get point submission detail (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Point submission retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point submission retrieved successfully"
                data:
                  id: 1
                  type: "OTC"
                  point: 100
                  price: 10000
                  nisData:
                    custServId: 12345
                    custId: "CUST001"
                    accountName: "PT Nusanet"
                    serviceCode: "SVC001"
                    serviceName: "Internet 50Mbps"
                    accountManager: "John Doe"
                    salesEmployeeId: "EMP-001"
                  status: "pending"
                  scheduleId: null
                  isAuto: false
                  period: null
                  notes: null
                  user:
                    id: 5
                    name: "Budi Santoso"
                  createdBy:
                    id: 1
                    name: "Admin Nusanet"
                  approvedBy: null
                  approvedAt: null
                  createdAt: "2024-06-01T07:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update a point submission (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type: { type: string, enum: [OTC, Bulanan] }
                price: { type: number, minimum: 0 }
                nisData:
                  type: object
                  properties:
                    custServId: { type: integer }
                    custId: { type: string }
                    accountName: { type: string }
                    serviceCode: { type: string }
                    serviceName: { type: string }
                    accountManager: { type: string, nullable: true }
                    salesEmployeeId: { type: string, nullable: true }
      responses:
        "200":
          description: Point submission updated successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point submission updated successfully"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"
    delete:
      summary: Delete a point submission (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Point submission deleted successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Point submission deleted successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  # =============================================
  # NIS ROUTES (Admin)
  # =============================================
  /nis/account:
    get:
      summary: Search NIS accounts from external database (admin only)
      tags: [PointSubmission]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string, minLength: 2 }
          description: Search keyword (min 2 characters)
          example: "nusanet"
      responses:
        "200":
          description: NIS accounts retrieved successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "NIS accounts retrieved successfully"
                data:
                  - custServId: 12345
                    custId: "CUST001"
                    accountName: "PT Nusanet"
                    serviceCode: "SVC001"
                    serviceName: "Internet 50Mbps"
                    accountManager: "John Doe"
                    salesEmployeeId: "EMP-001"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required

  # =============================================
  # ROLE MANAGEMENT ROUTES (Admin)
  # =============================================
  /role:
    get:
      summary: List roles (admin only)
      description: "Retrieve a paginated list of roles with employee counts. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/SearchParam"
      responses:
        "200":
          description: Roles retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/RoleListItem"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
              example:
                success: true
                statusCode: 200
                message: "Roles retrieved successfully"
                data:
                  - id: 1
                    name: "Super Admin"
                    description: "Full access"
                    color: "#FF5733"
                    permissions:
                      dashboard: ["L"]
                      user: ["L", "T", "E", "H"]
                    isDefault: false
                    employeeCount: 3
                    createdAt: "2024-06-01T08:00:00.000Z"
                    updatedAt: "2024-06-01T08:00:00.000Z"
                meta:
                  total: 5
                  perPage: 10
                  currentPage: 1
                  lastPage: 1
                  from: 1
                  to: 5
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions
    post:
      summary: Create a role (admin only)
      description: "Create a new role with permissions. Optionally assign employees. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoleRequest"
      responses:
        "201":
          description: Role created successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/RoleListItem"
              example:
                success: true
                statusCode: 201
                message: "Role created successfully"
                data:
                  id: 10
                  name: "Editor"
                  description: "Can edit content"
                  color: "#00FF00"
                  permissions:
                    education: ["L", "T", "E"]
                    catalog: ["L"]
                  isDefault: false
                  employeeCount: 0
                  createdAt: "2024-06-01T08:00:00.000Z"
                  updatedAt: "2024-06-01T08:00:00.000Z"
        "400":
          description: Bad request (e.g. duplicate role name)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 400
                message: "Role name already exists"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"

  /role/{id}:
    get:
      summary: Get role by ID (admin only)
      description: "Retrieve a single role with its assigned employees. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Role retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/RoleDetail"
              example:
                success: true
                statusCode: 200
                message: "Role retrieved successfully"
                data:
                  id: 1
                  name: "Super Admin"
                  description: "Full access"
                  color: "#FF5733"
                  permissions:
                    dashboard: ["L"]
                    user: ["L", "T", "E", "H"]
                  isDefault: false
                  employeeCount: 2
                  employees:
                    - id: 1
                      name: "Admin Nusanet"
                      email: "admin@nusanet.id"
                      photo: null
                    - id: 2
                      name: "Manager Nusanet"
                      email: "manager@nusanet.id"
                      photo: "https://cdn.example.com/photo.jpg"
                  createdAt: "2024-06-01T08:00:00.000Z"
                  updatedAt: "2024-06-01T08:00:00.000Z"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions
        "404":
          $ref: "#/components/responses/NotFoundResponse"
    put:
      summary: Update a role (admin only)
      description: "Update role name, description, color, permissions, or employee assignments. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoleRequest"
      responses:
        "200":
          description: Role updated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/RoleListItem"
              example:
                success: true
                statusCode: 200
                message: "Role updated successfully"
                data:
                  id: 1
                  name: "Super Admin"
                  description: "Updated description"
                  color: "#0000FF"
                  permissions:
                    dashboard: ["L"]
                    user: ["L", "T", "E", "H"]
                  isDefault: false
                  employeeCount: 2
                  createdAt: "2024-06-01T08:00:00.000Z"
                  updatedAt: "2024-06-02T10:00:00.000Z"
        "400":
          description: Bad request (e.g. duplicate role name)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
              example:
                success: false
                statusCode: 400
                message: "Role name already exists"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions
        "404":
          $ref: "#/components/responses/NotFoundResponse"
        "422":
          $ref: "#/components/responses/ValidationErrorResponse"
    delete:
      summary: Delete a role (admin only)
      description: "Delete a role and unassign all employees from it. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
          example: 1
      responses:
        "200":
          description: Role deleted successfully
          content:
            application/json:
              example:
                success: true
                statusCode: 200
                message: "Role deleted successfully"
                data: null
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions
        "404":
          $ref: "#/components/responses/NotFoundResponse"

  /role/permission-matrix:
    get:
      summary: Get permission matrix (admin only)
      description: "Retrieve all available permission modules with their keys, labels, groups, and allowed actions. Used for building the role permission UI. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Permission matrix retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PermissionModule"
              example:
                success: true
                statusCode: 200
                message: "Permission matrix retrieved successfully"
                data:
                  - key: "dashboard"
                    label: "Dashboard Overview"
                    group: "Beranda"
                    actions: ["L"]
                  - key: "user"
                    label: "Daftar Referral"
                    group: "Referral"
                    actions: ["L", "T", "E", "H"]
                  - key: "catalog"
                    label: "Katalog Reward"
                    group: "Katalog Reward"
                    actions: ["L", "T", "E", "H"]
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions

  /role/employees:
    get:
      summary: List employees for role assignment (admin only)
      description: "Retrieve all active employees with their current role assignments. Used in the role assignment UI. Admin only."
      tags: [Role]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Employees retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/BaseResponse"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/RoleEmployee"
              example:
                success: true
                statusCode: 200
                message: "Employees retrieved successfully"
                data:
                  - id: 1
                    name: "Admin Nusanet"
                    email: "admin@nusanet.id"
                    photo: null
                    roleId: 1
                  - id: 2
                    name: "Manager Nusanet"
                    email: "manager@nusanet.id"
                    photo: "https://cdn.example.com/photo.jpg"
                    roleId: 2
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "403":
          description: Forbidden - admin role required or insufficient permissions

tags:
  - name: Auth
    description: Authentication — register, login, logout, token refresh, and password reset
  - name: Profile
    description: Manage authenticated user's profile, bank info, and preferences
  - name: Point
    description: View current point balance
  - name: Point
    description: View and create reward point transactions
  - name: Redemption
    description: Create and view redemption requests (cash, voucher, product)
  - name: Customer
    description: View customers associated with the authenticated partner
  - name: CustomerService
    description: View customer service subscriptions
  - name: Service
    description: Browse internet/service packages offered by Nusanet
  - name: Template
    description: Download marketing template assets (PNG, JPG, PSD, MP4)
  - name: Catalog
    description: Browse items available for point redemption
  - name: Education
    description: Browse educational articles and videos
  - name: Statistic
    description: Dashboard statistics and chart data
  - name: Additional
    description: Lookup/reference data for dropdowns and filters
  - name: Notification
  - name: Feedback
    description: Submit and retrieve user feedback stored via Google AppScript
  - name: User
    description: Admin endpoints for managing and listing users
  - name: PointSubmission
    description: Admin endpoints for managing point submissions and NIS account search
  - name: Role
    description: Admin endpoints for managing roles, permissions, and employee assignments
