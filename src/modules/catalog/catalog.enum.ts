export enum CatalogType {
    VOUCHER = "voucher",
    PRODUCT = "product"
}

export enum StockHistoryType {
    INITIAL = "initial",
    ADDITION = "addition",
    REDUCTION = "reduction",
    OPNAME = "opname",
    REDEMPTION = "redemption"
}
